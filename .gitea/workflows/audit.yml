debianname: audit

on:
  push:
  workflow_dispatch:

jobs:
  audit:
    runs-on: debian

    steps:
      - uses: actions/checkout@v4

      - run: npm install

      - name: Audit dependencies
        run: npm audit || true
