gitea-runnername: audit

on:
  push:

jobs:
  audit:
    runs-on: gitea-runner

    steps:
      - uses: actions/checkout@v4

      - run: npm install

      - name: Audit dependencies
        run: npm audit || true
