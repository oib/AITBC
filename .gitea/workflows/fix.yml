name: autofix

on:
  workflow_dispatch:

jobs:
  fix:
    runs-on: debian 

    steps:
      - uses: actions/checkout@v4

      - run: npm install

      - name: Auto fix vulnerabilities
        run: npm audit fix || true
