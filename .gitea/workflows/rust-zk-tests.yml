name: Rust ZK Components Tests

on:
  push:
    branches: [main, develop]
    paths:
      - 'dev/gpu/gpu_zk_research/**'
      - '.gitea/workflows/rust-zk-tests.yml'
  pull_request:
    branches: [main, develop]
  workflow_dispatch:

concurrency:
  group: rust-zk-tests-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test-rust-zk:
    runs-on: debian
    timeout-minutes: 15

    env:
      WORKSPACE: /var/lib/aitbc-workspaces/rust-zk-tests

    steps:
      - name: Clone repository
        run: |
          rm -rf "${{ env.WORKSPACE }}"
          mkdir -p "${{ env.WORKSPACE }}"
          cd "${{ env.WORKSPACE }}"
          git clone --depth 1 --single-branch --branch ${GITHUB_REF_NAME:-main} http://aitbc:h2S%21%23.d9S%26%40h4%3f%3e@gitea.bubuit.net:3000/oib/aitbc.git repo

      - name: Initialize job logging
        run: |
          cd "${{ env.WORKSPACE }}/repo"
          bash scripts/ci/setup-job-logging.sh

      - name: Setup Rust environment
        run: |
          cd "${{ env.WORKSPACE }}/repo"
          
          # Ensure standard directories exist
          mkdir -p /var/lib/aitbc/data /var/lib/aitbc/keystore /etc/aitbc /var/log/aitbc
          
          export HOME=/root
          export RUSTUP_HOME="$HOME/.rustup"
          export CARGO_HOME="$HOME/.cargo"
          export PATH="$CARGO_HOME/bin:$PATH"

          if ! command -v rustup >/dev/null 2>&1; then
            echo "Installing Rust..."
            curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
          fi
          source "$CARGO_HOME/env"
          rustup default stable
          rustc --version
          cargo --version
          rustup component add rustfmt clippy

      - name: Check formatting
        run: |
          export HOME=/root
          export PATH="$HOME/.cargo/bin:$PATH"
          source "$HOME/.cargo/env" 2>/dev/null || true
          cd "${{ env.WORKSPACE }}/repo/dev/gpu/gpu_zk_research"
          cargo fmt --all -- --check
          echo "✅ Formatting OK"

      - name: Run Clippy
        run: |
          export HOME=/root
          export PATH="$HOME/.cargo/bin:$PATH"
          source "$HOME/.cargo/env" 2>/dev/null || true
          cd "${{ env.WORKSPACE }}/repo/dev/gpu/gpu_zk_research"
          cargo clippy --all-targets -- -D warnings
          echo "✅ Clippy OK"

      - name: Build
        run: |
          export HOME=/root
          export PATH="$HOME/.cargo/bin:$PATH"
          source "$HOME/.cargo/env" 2>/dev/null || true
          cd "${{ env.WORKSPACE }}/repo/dev/gpu/gpu_zk_research"
          cargo build --release
          echo "✅ Build completed"

      - name: Run tests
        run: |
          export HOME=/root
          export PATH="$HOME/.cargo/bin:$PATH"
          source "$HOME/.cargo/env" 2>/dev/null || true
          cd "${{ env.WORKSPACE }}/repo/dev/gpu/gpu_zk_research"
          cargo test --all-targets
          echo "✅ Tests passed"

      - name: Cleanup
        if: always()
        run: rm -rf "${{ env.WORKSPACE }}"
