name: Dependency Security Automation

on:
  push:
    branches: [main, develop]
    paths:
      - 'poetry.lock'
      - 'requirements.txt'
      - 'requirements-*.txt'
      - 'pyproject.toml'
      - '.github/workflows/dependency-security.yml'
  pull_request:
    branches: [main, develop]
  schedule:
    # Run daily at 2 AM UTC
    - cron: '0 2 * * *'
  workflow_dispatch:

jobs:
  dependency-scan:
    name: Dependency Security Scan
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
      pull-requests: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Install poetry
        run: |
          python -m pip install --upgrade pip
          pip install poetry==2.4.1

      - name: Install project dependencies
        run: poetry install --no-interaction

      - name: Run pip-audit
        run: |
          echo "=== pip-audit Security Scan ==="
          poetry run pip-audit -r requirements.txt --desc
          poetry run pip-audit -r requirements.txt --format json --output pip-audit-report.json

      - name: Run safety check
        run: |
          echo "=== Safety Security Scan ==="
          poetry run safety check --file requirements.txt --json --output safety-report.json
          poetry run safety check --file requirements.txt --output safety-report.txt

      - name: Upload security reports
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: security-reports
          path: |
            pip-audit-report.json
            safety-report.json
            safety-report.txt
          retention-days: 30

      - name: Create security issue if vulnerabilities found
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            let safetyReport = '';
            let pipAuditReport = '';

            try {
              safetyReport = fs.readFileSync('safety-report.txt', 'utf8');
            } catch (e) {}

            try {
              pipAuditReport = fs.readFileSync('pip-audit-report.json', 'utf8');
            } catch (e) {}

            const body = `
            ## 🔒 Dependency Security Vulnerabilities Detected

            Automated security scan detected vulnerabilities in project dependencies.

            ### Safety Report:
            \`\`\`
            ${safetyReport || 'No safety report available'}
            \`\`\`

            ### pip-audit Report:
            \`\`\`
            ${pipAuditReport || 'No pip-audit report available'}
            \`\`\`

            ### Recommended Actions:
            1. Review the vulnerabilities above
            2. Update affected dependencies to secure versions
            3. Run \`poetry update <package>\` to update
            4. Regenerate requirements.txt/requirements-dev.txt with scripts/ci/export-requirements.sh
            5. Commit the updated poetry.lock and exported requirements files
            `;

            github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: '🔒 Security: Dependency Vulnerabilities Detected',
              body: body,
              labels: ['security', 'dependencies', 'automated']
            });

  dependency-update:
    name: Automated Dependency Updates
    runs-on: ubuntu-latest
    if: github.event_name == 'schedule'
    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Install poetry
        run: |
          python -m pip install --upgrade pip
          pip install poetry==2.4.1

      - name: Check for outdated dependencies
        run: |
          echo "=== Checking for outdated dependencies ==="
          poetry show --outdated --no-interaction > outdated.txt || true
          cat outdated.txt || echo "No outdated packages found"

      - name: Update dependencies with safety check
        run: |
          echo "=== Updating dependencies with Poetry ==="

          # Update lockfile from pyproject.toml constraints
          poetry update --no-interaction

          # Regenerate pinned requirements from the resolved lockfile
          poetry export --only main --without-hashes -o requirements.txt
          poetry export --only dev --without-hashes -o requirements-dev.txt

          # Security check on the resolved dependency set
          echo "=== Security check on updated dependencies ==="
          poetry run safety check --file requirements.txt

          # Show changes
          echo "=== Dependency Changes ==="
          git diff -- poetry.lock requirements.txt requirements-dev.txt || true

      - name: Create PR if updates available
        id: create-pr
        run: |
          if [ -n "$(git status --porcelain poetry.lock requirements.txt requirements-dev.txt)" ]; then
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git checkout -b deps/update-$(date +%Y%m%d)
            git add poetry.lock requirements.txt requirements-dev.txt
            git commit -m "deps: update dependencies [skip ci]"
            git push origin deps/update-$(date +%Y%m%d)

            echo "created=true" >> $GITHUB_OUTPUT
          else
            echo "No dependency updates needed"
            echo "created=false" >> $GITHUB_OUTPUT
          fi

      - name: Create Pull Request
        if: steps.create-pr.outputs.created == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.pulls.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: 'deps: automated dependency updates',
              head: 'deps/update-' + new Date().toISOString().slice(0,10).replace(/-/g,''),
              base: 'main',
              body: '## 🤖 Automated Dependency Updates\n\nThis PR updates project dependencies to their latest versions.\n\n### Security Check\nAll updated dependencies have been scanned for known vulnerabilities using safety.\n\n### Testing\nPlease test thoroughly after merging these updates.',
              labels: ['dependencies', 'automated']
            });

  security-scorecard:
    name: OSSF Scorecard Analysis
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Run Scorecard analysis
        uses: ossf/scorecard-action@v2.3.3
        with:
          results_file: results.sarif
          results_format: sarif
          publish_results: true

      - name: Upload SARIF results
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: results.sarif
