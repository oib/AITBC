# =============================================================================
# Harness Manifest - generated by scripts/setup-template.sh (bootstrap)
# =============================================================================
# Declares how this fork customizes the upstream SAFe Agentic Workflow harness.
# See docs/HARNESS_MANIFEST_SCHEMA.md and .harness-manifest.schema.json.
# Regenerate richer sync metadata with:
#   ./scripts/sync-claude-harness.sh manifest init
# =============================================================================

manifest_version: "1.1"

identity:
  PROJECT_NAME: "AITBC"
  PROJECT_REPO: "AITBC"
  PROJECT_SHORT: "AITBC"
  GITHUB_ORG: "oib"
  TICKET_PREFIX: "AITBC"
  MAIN_BRANCH: "main"
  COMPANY_NAME: "AITBC"
  AUTHOR_NAME: "oib"
  AUTHOR_FIRST_NAME: "oib"
  AUTHOR_LAST_NAME: "oib"
  AUTHOR_HANDLE: "oib"
  AUTHOR_EMAIL: "andreas.fleckl@chello.at"
  SECURITY_EMAIL: "andreas.fleckl@chello.at"
  ARCHITECT_GITHUB_HANDLE: "oib"
  MCP_LINEAR_SERVER: "linear-mcp"
  MCP_CONFLUENCE_SERVER: "confluence-mcp"
  MCP_JIRA_SERVER: "jira-mcp"
  DB_USER: "aitbc"
  DB_NAME: "aitbc"

# Rename mappings: upstream path -> local path (repo-root-relative in v1.1).
renames: {}

# Protected files: never overwritten during sync.
protected: []

# Replaced files: fork maintains independently.
replaced: []

# Sync behavior preferences.
sync:
  sync_scope:
    - ".claude/"
    - ".gemini/"
    - ".codex/"
    - ".cursor/"
    - ".agents/"
    - "dark-factory/"
  auto_substitute: true
  backup: true
  conflict_strategy: "prompt"
