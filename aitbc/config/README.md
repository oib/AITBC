# aitbc.config

Configuration management for AITBC applications.

## Exports

- `HierarchicalConfig` - Hierarchical configuration with validation
- `ValidatedAITBCConfig` - Validated configuration with defaults

## Usage

```python
from aitbc.config import HierarchicalConfig
```
