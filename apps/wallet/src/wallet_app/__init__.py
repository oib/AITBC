"""Wallet daemon FastAPI application package."""

# Note: Do not import from .main here to avoid circular import warnings
# when running with `python -m wallet_app.main`.
# Use `from wallet_app.main import create_app` or `from wallet_app.main import app` directly instead.

__all__ = []
