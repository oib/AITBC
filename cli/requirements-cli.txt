# AITBC CLI Requirements
# CLI-specific dependencies, pinned to the versions resolved in poetry.lock.
# Core runtime dependencies are in /opt/aitbc/requirements.txt.

click==8.4.1
rich==15.0.0
tabulate==0.10.0
colorama==0.4.6
keyring==25.7.0
typer==0.25.1
websockets==14.2
