# AITBC CLI Requirements
# CLI-specific dependencies (use central system for other dependencies)

# CLI Enhancement Dependencies
click>=8.1.0
rich>=13.0.0
tabulate>=0.9.0
colorama>=0.4.4
keyring>=23.0.0
click-completion>=0.5.2
typer>=0.12.0

# Installation: Use central requirements system
# Core dependencies: pip install -r /opt/aitbc/requirements.txt
# Dev dependencies: pip install -r /opt/aitbc/requirements-dev.txt
# Or use installation script: ./scripts/deployment/install-profiles.sh <profile>
# Available profiles: server-no-gpu, hub, customer-no-gpu, provider-gpu
