# 2026-09-07 — CI green, redacted MCP bridge-status tool, node1 outage recovery

## CI / candidate verification

- Commits `39881c7830` and `b62637a792` pushed to Gitea `main` and GitHub mirror.
- [Gitea CI run 19639](https://gitea.bubuit.net/oib/aitbc/actions/runs/19639) on `b62637a792` completed successfully (8m).
- Gates executed: `lint-strict`, `c901-ratchet`, `no-float-money`, `typecheck`, `test`, `test-apps`, `test-cli`, `test-governance`, OpenAPI drift, version consistency, live dry-run.
- Redacted MCP bridge-status tool added in `mcp-server/aitbc_mcp_rpc_tools.py` with regression tests in `mcp-server/tests/test_aitbc_mcp_rpc_tools.py`.
- `make test` now includes `mcp-server/tests`.
- MyPy typecheck passed after baseline adjustment and `opentelemetry.trace` import fix in `apps/blockchain-node/src/aitbc_chain/observability/exporters.py`.

## node1 validator outage / recovery exercise (C-2 / R6)

- **Approved scope:** stop only `aitbc-blockchain-node.service` on node1; keep `aitbc-blockchain-rpc` and other services running.
- **Pre-outage fleet state (22:58:50+02:00):**
  - hub, hub2, node1, node2 at height **4618**, hash `0xf32ecb0fa2f3f2cb55f58d8dfebe0cd6ce86a02a410951178f115cb4fe5d17f2`.
  - node0 at height **4616**, different hash `0xb17c...` and `genesis_params.block_time_seconds: 10` (config reporting drift, but block data converged later).
- **Outage:** `systemctl stop aitbc-blockchain-node` on node1 at **22:58:51**. Service `inactive`.
- **During outage:**
  - 22:59:36: surviving validators still at 4618.
  - 23:00:28: still at 4618.
  - 23:01:19: surviving validators produced block **4619**, hash `0xe88e87a80bd4d07e647c838ba8a8ff539e54d69cef4227901308ff00848e7f8e`.
- **Restart:** `systemctl start aitbc-blockchain-node` at **23:01:25**. Service `active`.
- **Rejoin:**
  - 23:01:58: node1 at **4620** matching hub/hub2/node2.
  - 23:02:33: hub/hub2/node2 at **4621**, hash `0xef6bfd518cb20c10776fe9f4731bcc94f561ae720623c8263052c3ef56527415`; node1 still at 4620.
  - 23:03:07: node1 caught up to **4621** matching hash.
- **Final fleet state (23:03:51):**
  - hub, hub2, node1, node2, node0 all at height **4621**, hash `0xef6bfd518cb20c10776fe9f4731bcc94f561ae720623c8263052c3ef56527415`.
- **Conclusion:** 3-of-4 validators sustained block production while node1 was down; node1 rejoined within ~2 minutes and converged.

## Bridge (A-2) end-to-end ETH→AIT round-trip

- Enabled `BRIDGE_ENABLED=true` in `/etc/aitbc/exchange.env` on `node2` after operator approval and restarted `aitbc-wallet`.
- Set `GENESIS_WALLET_ADDRESS` to the address whose key is in `/etc/aitbc/node.env` (`0xbDCd234aCB32cDd876CD3691515ea91a620793e2`) and `BLOCKCHAIN_RPC_URL=https://hub.aitbc.bubuit.net` so the monitor submits mint transactions to the proposer.
- Updated `ETH_RPC_URL` in `/etc/aitbc/exchange.env` to `https://ethereum-sepolia-rpc.publicnode.com` because `eth.llamarpc.com` returned SSL 525 from node2.
- Deposit sent from hub `test-bridge-deposit` wallet (`0x72184bFF2Ad4081347a00B26712D90bf49644E97`, the hub wallet with test ETH) using:
  ```
  aitbc market --wallet-path /var/lib/aitbc/wallets/test-bridge-deposit.json exchange deposit-eth \
    --amount 0.001 \
    --ait-address 0x3Ed42960a36489Fe1BA39ceCcbbd6F87C8551Ebf \
    --bridge-address 0x322996afC154312E04F76d241F972DfF011Af7df
  ```
- **Sepolia ETH deposit tx:** `0xb417a0f715c3b89ae2ef6afb57f73e5099fa7b85954f69be73310a56d0ecd3dd` (confirmed in Sepolia block `0xb1de2a`).
- Bridge monitor on `node2` detected the deposit and minted `2.49425` AIT.
- **AIT mint tx:** `0xd13b9cf19f9796405a77e7c255f6b5c759d82ed98978598916bbe4ba6f073068`, included in AIT block **4640**.
- **Recipient `0x3Ed42960a36489Fe1BA39ceCcbbd6F87C8551Ebf` balance:** `89,793,000` compute-units (2.49425 AIT); `0xbDCd...` mint source nonce advanced from 3 to 4.
- Round-trip confirmed: `0.001` Sepolia ETH → `2.49425` AIT on `ait-hub.aitbc.bubuit.net`.

## Findings

- node0 reports `genesis_params.block_time_seconds: 10` while the rest of the fleet reports `60`. At the end of the exercise its block height and hash matched the fleet, so it appears to be on the same chain with a local config/reporting drift. Investigate node0's `BLOCK_TIME` / genesis params separately.
- Bridge end-to-end ETH→AIT round-trip succeeded on 7 Sep after enabling the bridge and correcting `ETH_RPC_URL` and `BLOCKCHAIN_RPC_URL`. Config changes are live on `node2`; the wallet must be restarted from any future source upgrade to preserve them.

## Redacted MCP bridge-status tool deployed

- Pulled `b62637a792` to `node2:/opt/aitbc` and restarted `aitbc-wallet`.
- Restarted the IDE MCP server so the new `get_eth_bridge_status_redacted` tool is loaded.
- Verified `get_eth_bridge_status_redacted` (role `shop`) returns:
  - `status`, `message`, `enabled`, `network`, `deposit_address`, `fee_rate`, `min_deposit`, `poll_interval`, `auto_poll`
  - **no `rpc_url` field.**
- The non-redacted `get_eth_bridge_status` tool and the wallet's `GET /v1/bridge/status` endpoint still return `rpc_url`; the new public URL is `https://ethereum-sepolia-rpc.publicnode.com` (no API key), so it is no longer a credential-bearing leak, but the raw endpoint still exposes infrastructure detail.

## S-3/R1 — auto-adjudication trust boundary closed in code

- Commit `6651dc3b1` on gitea `main`.
- Removed `shadow_mode`, `spot_check_for`, `spot_check_result` from client `Constraints`.
- `JobService.create_job` strips these server-only keys from client input.
- `SpotCheckService` now writes the authoritative `spot_check_result` only to the server-created shadow `Job`.
- `PaymentService.get_dispute_evidence` looks up the completed shadow job, verifies `shadow_mode`, and checks the record's `spot_check_job_id` and `original_job_id`.
- Added `test_client_forged_spot_check_result_is_ignored` regression.
- `make lint-strict`, `make no-float-money`, `make typecheck`, `make test-apps` passed on `node2` before push; OpenAPI spec regenerated; Gitea CI run **19640** green.
- **Deployment 8 Sep:** `aitbc-coordinator-api.service` on `hub.aitbc` restarted with gitea `main` at `c79462f3e`. `/health` returns `ok`, `journalctl` shows no errors, live `/openapi.json` confirms `Constraints` no longer contains `shadow_mode`.
- **Residual:** no live dispute → shadow re-run → auto-adjudication end-to-end exercise yet; requires separately approved live validation.

## Artifacts

- Raw outage evidence log: `/tmp/node1_outage_evidence_20260907_225846.log` on this host.
