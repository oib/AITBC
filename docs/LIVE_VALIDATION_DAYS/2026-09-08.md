# 2026-09-08 — B-8 schema conformance and mypy baseline follow-up

## B-8 closed in code

- Commit `3d09e8105` on gitea `main`.
- `001_initial_migration` now imports `coordinator_api.models.multitenant` in `upgrade` and `downgrade`, ensuring the 8 tenant tables are created on fresh `alembic upgrade head`.
- `apps/coordinator-api/tests/integration/test_b8_schema_conformance.py` imports the same model module in `_get_model_columns`, making the expected metadata deterministic and independent of pytest collection order.
- `Makefile::test-apps` now includes `apps/coordinator-api/tests/integration`, so `test_b8_schema_conformance.py` and `test_migrations.py` run in CI.
- `make lint-strict`, `make typecheck`, `make no-float-money`, `make test-apps` passed on `node2`.
- Gitea CI run **19643** on `c79462f3e` completed successfully.
- **Residual:** historical-schema-to-head upgrade regression is not implemented; requires separate design/approval.

## D-2 re-derived as closed on `main`

- The cited `761b887837` exists only on `origin/feat/open-island-gossip-remediation`, but `git diff 252469f3b 761b88783` shows an identical tree.
- `252469f3b`, `b990194ed7`, and `20257c5975` are on `main`.
- `apps/blockchain-node/tests/test_websocket.py`, `test_gossip_broadcast.py`, and `test_network_info.py` pass on `node2`.
- ~~The `feat/open-island-gossip-remediation` branch still carries four unmerged commits (CLI escrow guard and CI contract-suite fixes) unrelated to the gossip auth gap.~~ **Correction (late-8-Sep re-verify):** the branch carried zero unmerged substance — `f8a09b547c` and `886b10b79` are byte-identical on `main`, the CI commits are superseded by effect — and the branch no longer exists on gitea. See MEGAPLAN §17.3.

## O-3 re-audit

- `/etc/aitbc/redis.env` exists on `hub.aitbc` and `node2`; absent on `hub2.aitbc`, `node0`, `node1`.
- No `aitbc-cache-monitor.service` is installed on any host, so `redis.env` is not loaded by any live unit.
- The actual Redis password is present in `/etc/aitbc/blockchain-secrets.env` (followers) and `redis.conf` (`hub.aitbc`); the `redis.env` file appears to be a `redis-cli` convenience, not a service dependency.
- O-3 remains narrowed: operator should confirm whether `redis.env` is vestigial or whether it should be distributed/loaded.

## CI red-streak audit via `tea api` on `hub.aitbc`

- ~~Authenticated Gitea Actions API shows 39 consecutive failed runs from 19600 to 19638.~~ **Correction (late-8-Sep re-verify):** the real figure is 162 failed runs (19477–19638, ~70 hours); the 22/39 counts were API page-size artifacts. See MEGAPLAN §17.3.
- 19639 (`b62637a792`) is the first green run in the window.
- 19642 (`3d09e81054`) failed; the B-8 follow-up 19643 (`c79462f3e`) is green.
- 19644–19646 are green.

## F-5f closed in code

- Commit `8cb48178d` on gitea `main`.
- Added `contracts/test/EscrowService.test.js` (23 tests) and `contracts/contracts/mocks/MockEnergyPricing.sol`.
- Fixed `EscrowService.createEscrow` to persist `_releaseTime` (time-based release/refund now work correctly).
- `npx hardhat test` passes 182 mocha tests (159 before).
- `make lint-strict`, `make typecheck`, `make no-float-money` passed on `node2`.
- Gitea CI run **19646** on `8cb48178d` completed successfully.
- **Residual:** other Solidity contracts still lack named coverage; `AgentStaking` already has 53 hardhat tests.

## S-4 closed in code

- Commit `d119e8ca9` on gitea `main` (S-4 implementation started at `f1b0d465a`; default-block-version fix at `d119e8ca9`).
- Added `state_transition_v3_height` and `escrow_settlement_authority` to `ChainSettings`.
- Proposer now stamps `state_transition_version` via `get_block_version_for_height` and applies transactions with the same version.
- v3 `ESCROW_RELEASE`/`ESCROW_REFUND` validate the original lock, bind the beneficiary, and enforce the settlement authority.
- Cross-activation rule: v2 escrows (funds in node wallet) can still be released/refunded after v3 activation; v3 escrows use the per-escrow address.
- `apps/blockchain-node/tests/test_s4_per_escrow.py` covers v3 lock/release/refund, beneficiary/signer rejection, v2→v3 cross-activation, and block-version helpers.
- `make lint-strict`, `make typecheck`, `make no-float-money`, `make test-apps` passed on `node2`.
- Gitea CI run **19645** on `d119e8ca9` completed successfully.
- **Residual:** `state/pure_state_transition.py::compute_state_delta` has no v3 per-escrow logic (falls back to sequential); live v3 activation requires operator to set a positive `STATE_TRANSITION_V3_HEIGHT` and restart blockchain nodes.

## mypy baseline follow-up

- The `5cafb57ea` baseline update removed two `unused-ignore` entries that CI 19641 re-reported because the CI venv lacks `torch` while dev nodes have it.
- Commit `c79462f3e` adds per-file `# mypy: warn_unused_ignores=False` to `engine.py` and `fusion_engine.py`, keeping the same code passing in both environments.
- Baseline stays at 6 known errors.

## O-1 / R8 — node2 GitHub PAT revocation confirmed

- The operator confirmed on 8 Sep that node2's GitHub personal access token is revoked at github.com/settings/tokens.
- Recorded as operator attestation only, per the R8 exit gate: the token value was not requested, read, copied, or tested.
- Local cache clearance / no stored credentials on node2 had already been recorded 7 Sep; this closes the remaining provider-side confirmation.
- R8 / O-1 is closed in the register.

## R10 — fleet manifest and audit-trail evidence refresh (read-only)

- Re-swept all five hosts ~13:15 CEST: HEAD `6e6bb21615` on `main`, matching each host's stored `origin/main`; all `/health` endpoints return `ok`; fleet converged at height 5246, hash `0x6babc0fbec351ed8…`; zero failed `aitbc-*` units.
- Gitea CI run **19651** on `6e6bb21615` is green. The three commits since the 10:40 manifest (`b57c2e4545`, `1b33979708`, `6e6bb21615`) touch only `docs/releases/v0.25/v0.25.5_change.log`, so services last restarted 7–8 Sep are not behind on runtime code.
- node2 has 2 uncommitted files: `consensus/poa.py` +20/−4 and untracked `tests/consensus/test_proposer_watchdog.py` — in-progress R7 work in a concurrent session, not deployed code.
- Coordinator schema: the live store is `/var/lib/aitbc/data/coordinator.db` on `hub.aitbc` (identified via the coordinator-api process's open fd), at `alembic_version = f53990f9d6cc` — the §9 target head. The 0-byte `/opt/aitbc/data/coordinator.db` and `/var/lib/aitbc/coordinator.db` are not the live store.
- Backups: `/var/backups/aitbc/20260908_010*` present on all five hosts (daily job). hub's snapshot contains per-database `.gz` dumps plus `etc-aitbc.tar.gz`, `wallets.tar.gz`, `keystore.tar.gz`, `postgres_aitbc_{mempool,poolhub}.sql.gz`. Secret-bearing tarballs were not opened. This evidences the backup job running, not a restore test — none is recorded.
- Not established: a per-pull approval log for the advance to tip (fleet moved via fast-forward-only pulls); no restore-test evidence. Neither is manufactured here.

## ENERGY_OPERATOR_ADDRESS resolved on hub

- A dedicated energy-operator keypair was generated on `hub.aitbc` and written directly into `/etc/aitbc/aitbc-coordinator-api.env` (backup `aitbc-coordinator-api.env.bak-20260908-energyop`, perms 640 root:aitbc preserved). The private key never entered the transcript.
- `ENERGY_OPERATOR_ADDRESS = 0xD8ca07D43584509b715e2Be475a635D793f03E4d`.
- `aitbc-coordinator-api` restarted (pid 2097604); both vars confirmed in `/proc/<pid>/environ` by name; `/health` 200 on :8203; zero failed units.
- Effect: `marketplace_gpu` now signs energy quotes (`energy_operator_key` + address both set), and `payments.py` protected funding verifies instead of returning 503. Energy quotes get their own identity, separate from the `0xe738…d225` bond-slash authority question in the next entry.
- **Scope settled 8 Sep — one fleet-wide operator identity:** the same pair was deployed to node2's `aitbc-coordinator-api.env` (piped host-to-host, key never in transcript; the commented `0xe738…d225` block superseded), `aitbc-coordinator-api` restarted (pid 231264), health 200. The chain-side `escrow_routes` gate is armed **on hub only**: `ENERGY_OPERATOR_ADDRESS=0xD8ca…3E4d` added to `aitbc-blockchain-rpc.env`, `aitbc-blockchain-rpc` restarted (pid 2098198), `/health` ok, `/v1/height` serving, zero failed units. Follower RPCs remain permissive by operator choice — the coordinator submits funding through hub, so hub is the enforcement point.

## node0 block-lag — fixed sync source and block_time drift

- **Symptom:** node0 was consistently 2–3 blocks behind the rest of the fleet.
- **Root cause:** `default_peer_rpc_url` in `/etc/aitbc/blockchain.env` pointed to
  `https://node1.aitbc.bubuit.net` (a follower) while every other node pointed to
  `https://hub.aitbc.bubuit.net`. The file also had stale legacy variables
  (`SYNC_SOURCE_HOST=node1`, `SYNC_SOURCE_PORT=7070`, `SYNC_LEADER_HOST=node1`),
  `BLOCK_TIME=5`, and `market_role=shop`.
- **Fix applied 8 Sep:**
  - `default_peer_rpc_url=https://hub.aitbc.bubuit.net`
  - removed `SYNC_SOURCE_HOST/PORT/LEADER`
  - `BLOCK_TIME=5` replaced with `BLOCK_TIME_SECONDS=60`
  - `market_role=customer`
  - backup kept as `/etc/aitbc/blockchain.env.bak-20260908-node0sync`
  - `aitbc-blockchain-node` and `aitbc-blockchain-rpc` restarted.
- **Result:** after a transient 502 while hub reloaded, node0 subscribed to hub and
  caught up; fleet converged at height **5266** with zero failed `aitbc-*` units.

## BOND_SLASH_AUTHORITY_ADDRESS — fleet split fixed, config-only

- Derived the shared `BOND_SLASH_PRIVATE_KEY` on hub without printing it: it controls **`0xab0797Ae8cfF09B313c71cAb2f894B342b6e1d76`** — the only working slash authority. `0xe738…d225` (configured on hub/node1) is hub's proposer address; no key derives to it.
- Operator decision: **keep the key, fix config only** — the transcript exposure stands as accepted risk.
- `BOND_SLASH_AUTHORITY_ADDRESS=0xab07…1d76` now set fleet-wide: added on node0 (was unset — it would have skipped every slash), corrected in `blockchain.env` on node1 and hub, already correct on node2/hub2. Hub's double-definition (coordinator env vs `blockchain.env` override) is neutralized — both now agree.
- Rolling restarts: node0 and node1 (`aitbc-blockchain-node` + `-rpc`), hub (node + rpc + coordinator-api). Verified in `/proc/<pid>/environ`; fleet converged at height 5265 `0x1e5e9154…`; zero failed units.
- Residual: the key's value persists in `.bak` / `.secret-migration-backup-*` files and daily `etc-aitbc.tar.gz` archives on hub/node2 — inert on-chain while the key is unchanged. **Closed under the operator's stated dev-fleet posture (8 Sep):** credential-hygiene findings are recorded, not remediated, unless they break function or the operator asks — rotation plus purge is not planned work.

## O-3 — redis.env confirmed needed, monitor installed

- Operator disposition: `redis.env` is **needed** — it supplies `REDISCLI_AUTH` to `aitbc-cache-monitor.service` (`EnvironmentFile=-`, optional by design).
- The real gap was that the unit was never installed anywhere — and worse, the role-aware `link-systemd.sh` deletes all `aitbc-*` links and relinks only role services, so hand-installed monitor units would not survive an `update.sh`.
- **Correction to the earlier audit:** redis-server runs on **all five hosts** (it is the gossip backend); auth (`requirepass`) is configured only on hub + node2, which is why `redis.env` exists only there. Monitor installed + timer enabled on all 5; first runs `Result=success` (hit rates 100%/98%/99%/0%/15% — cold caches on low-traffic nodes are advisory).
- **Durability fix committed on node2 as `7d93b678a`** (note: the commit landed with a mislabeled `docs(release)` subject from a raced concurrent commit — content is the script changes + a changelog file):
  - `link-systemd.sh`: `aitbc-cache-monitor` allowed whenever a `redis-server` unit exists, gated via `systemctl cat` (a `list-unit-files|grep -q` pipe reports 141 under `pipefail` — SIGPIPE — and silently skipped the unit; found during verification on node2).
  - `setup.sh`: `ensure_service_env_files` creates `/etc/aitbc/redis.env` (0640 root:aitbc) on Redis hosts, populating `REDISCLI_AUTH` from `redis.conf` `requirepass` when configured; `setup_autostart` enables the timer when both redis-server and the linked timer exist.
- O-3 closed; the register is now fully closed for this pass.

## Bond-slash forensics resolved + deterministic-authority fix (`5c79eebe3`)

- **Question:** did the authority split predate the 1–2 Sep slashes? **Yes — and the divergence actually happened.** The old chain (pre-3-Sep-rebuild) snapshot shows node0 held all five BOND_SLASH txs in `transaction` but kept the three bonds `active` at full amounts (`slashed_tx_hash=NULL`) while hub/node1/node2/hub2 show `slashed`/reduced — same blocks, different state.
- **Timeline from backup env tars:** hub ran a third ghost authority `0xfe2d…9e03` on 1–3 Sep; `0xab07…1d76` appeared on node2+hub2 on 2 Sep, node1 on 3 Sep; node0 never configured it. The `0xe738…d225` mis-set on hub/node1 was introduced **8 Sep** (between the 01:00 backup and the 13:50 fix) — not related to the slash window.
- **No live repair needed:** the divergence died with the 3 Sep 20:48 chain rebuild; the current chain has zero slash history.
- **Code fix (`5c79eebe3`, on node2, all checks green):**
  - `_bond_slash_authority(session, chain_id)` reads the on-chain `bond_slash_authority` chain parameter first (deterministic, set via `GOVERNANCE_EXECUTE parameter_change`), env var remains fallback, disagreement logs a warning.
  - `_handle_bond_transaction` returns skip reasons; the apply site logs them at WARNING so a skipped slash can no longer hide behind "Applied transaction". Removed a duplicated "Bond slashed" log line.
  - `genesis.json` can carry a `parameters` map seeded into `chain_parameter` at block-0 creation (file + RPC-bootstrap paths).
  - New `scripts/monitoring/fleet-config-check.sh` diffs consensus-relevant env vars across hosts; first live run found only cosmetic drift (`BOND_BURN_ADDRESS` unset on node0 = the keccak-derived default).

## On-chain pin: `bond_slash_authority` (block 5336)

- Governance flow executed live: proposal `bond-slash-authority-pin-2026-09-08` → voted (genesis `0xFe2d…9e03`, quorum 0 default) → `GOVERNANCE_EXECUTE` tx `0xde13acc3…` signed by `0xab07…1d76` → applied in **block 5336** → `chain_parameter.bond_slash_authority = 0xab07…1d76` present in **all five hosts' chain DBs at height 5337**. Consensus-applied; env var now advisory once `5c79eebe3` code deploys.
- **Ghost identity resolved:** `0xFe2d63FE87Db282083b9159e5857Cac788af9E03` is the chain's `GENESIS_ADDRESS` — hub's `GENESIS_PRIVATE_KEY` signs it. On 1–3 Sep the genesis key doubled as slash authority; not an orphan.
- **New finding — mempool propagation hole:** REST `submit_transaction`/`execute_governance_proposal` only write the *local process* mempool; nothing publishes txs to the `transactions.<chain>` gossip topic (only the WS gossip endpoint does). hub's RPC broker has `GOSSIP_MESH_PEER_URLS=` empty, so a publish there never leaves hub. Result: REST-submitted txs mine only if the submitting host happens to propose. Workaround used: insert the signed tx into each proposer host's `mempool` DB directly (pg on hub/node2, sqlite chain.db on hub2). node1's `MEMPOOL_DB_URL` contains a literal `:password@` placeholder — its DB mempool is unreachable (worth fixing, likely falls back or errors on add).
- Stale rows left in hub's pg mempool pre-date this work (`0xd13b9cf1…` + 2 others) — not touched.

## Mempool propagation hole — fixed (`1d5b7bba6`, verified live)

- **Root cause confirmed:** only the *node* process runs `GOSSIP_BACKEND=mesh` (local redis + WS peers); every host's `aitbc-blockchain-rpc.env` ran `GOSSIP_BACKEND=redis` with `GOSSIP_MESH_PEER_URLS=` emptied — so REST submits landed in the local mempool DB and never left the host. Txs mined only when the submitting host proposed.
- **Fix:** `submit_transaction`, `submit_marketplace_transaction`, and `execute_governance_proposal` now call `_queue_peer_fanout`, which pushes `{"type":"new_transaction","tx":…}` to every `GOSSIP_MESH_PEER_URLS` peer's public `transactions.<chain>` WS gossip endpoint — one hop, tx-hash deduped, no loop risk. Tests: `tests/test_tx_peer_fanout.py` (publish-to-each-peer, no-peer no-op, peer-failure isolation).
- **Deployed:** all five hosts pulled to `1d5b7bba6`, empty `GOSSIP_MESH_PEER_URLS=` override removed from `aitbc-blockchain-rpc.env`, `aitbc-blockchain-rpc` restarted. node1's `MEMPOOL_DB_URL` placeholder (`:password@`) replaced with the sqlite chain.db backend; `aitbc-blockchain-node`+`rpc` restarted there.
- **Live proof:** MESSAGE tx `0x8fc821…` submitted via hub REST at ~height 5354 → found in node2's pg mempool via fan-out → **mined in block 5355 confirmed** — hub produces no blocks, so inclusion proves remote-proposer delivery.

## GOVERNANCE_EXECUTE gate (`628f99a74`) + `governance_executors` pin (block 5368)

- **Hole closed:** any funded account could rewrite chain parameters via GOVERNANCE_EXECUTE. `validate_transaction` now checks the sender against the on-chain `governance_executors` chain parameter (comma-separated, canonical addresses) — unset = permissive (pre-gate behavior), set = deterministic rejection on every node at the same height. Deliberately *not* env-based: a per-node list would recreate the slash-authority divergence class.
- **Pinned on-chain:** `governance_executors = 0xab07…1d76` applied in block 5368 on all five hosts. The genesis-signed execute path is **broken**: `GENESIS_ADDRESS=0xFe2d…` but `GENESIS_PRIVATE_KEY` derives to `0x2A9f…` — endpoint-signed executes are signature-invalid and can never apply (explains the two dead txs `0x3178…`/`0xfdd87f…`). No host's `/etc/aitbc/*.env` holds a key for `0xFe2d…` — bootstrap-era identity, key location unknown. Direct REST submission with the slash/proposer key is the working path.
- **Incident + recovery:** the rolling `aitbc-blockchain-node` restart raced node1's proposal round — node1 forked at height 5359 while the other four hosts stayed together. Recovered via snapshot: `reset-follower-to-snapshot.sh` couldn't ssh node1→hub (no root key), and `VACUUM INTO` produced a malformed-schema copy (an odd object in sqlite_master); the `.backup` online snapshot + manual transfer worked. node1 reconverged by 5365 (hash-matched hub). Its diverged chain DB preserved at `chain.db.pre-snapshot.20260908-161907`.
- **Test-tx housekeeping:** stale mempool rows cleaned; `TransactionRequest` mutates `payload` server-side (injects `to`/`amount` if absent) — clients must sign payload *including* those fields or REST returns 403 "Invalid transaction signature".

## B-8 residual closed: historical-schema upgrade regression (`0c460fe9f`)

- Exercised the real path: restored the **2026-08-09 `chain_coordinator.db` backup** (alembic stamp `236edfbd9728`, 145 tables, 24 rows) and ran `alembic upgrade head`.
- **Two real bugs found and fixed:**
  1. `5d8339a13a12` used `server_default=sa.text("[]")` — non-constant default sqlite rejects on `ALTER TABLE ADD COLUMN` → quoted to `'[]'`.
  2. The eight `tenant_*` tables exist only via `001_initial`'s `create_all` — a stamped historical DB could never gain them. New head migration `e7f2a9c4b1d0` creates them `checkfirst`.
- Post-fix: `236edfbd9728 → e7f2a9c4b1d0` converges — 8 tenant tables created, all 24 historical rows preserved, zero missing/extra tables vs declared models.
- CI regression test `tests/integration/test_b8_historical_upgrade.py`: freeze at pre-head rev + drop tenant tables + `upgrade head` → asserts full model conformance and sentinel survival.

## Mempool housekeeping + fleet state (end of day)

- hub's pg mempool cleared of 6 dead rows: two duplicate `bond_slash_authority` executes (superseded by applied `0xde13acc3`), two signature-invalid `governance_executors` executes from the broken `0xfe2d`/`0x2a9f` key mismatch, the already-mined `0xc0cf9979` pin, and a stale nonce-3 TRANSFER from `0xbdcd…` (account nonce already 4 — permanently unlandable since 7 Sep).
- Fleet converged at height 5404, identical head hash `0x748a06f8…` on all five hosts — node1's earlier fork fully recovered.
- Dispute-path live evidence confirmed as already existing: 5 Sep day file records job `e20c8fcb` exercising reject → disputed → admin refund ruling → on-chain refund (tx `0x0770a1060329…`) — the D2 "test-verified only" limitation is closed.
- `feat/open-island-gossip-remediation` branch no longer exists on gitea `origin` (only `main` remains) — the MEGAPLAN reference is historical.

## R10 partial: DB-level backup restore test (hub, 20260908_010420)

- Decompressed and imported tonight's dumps into scratch DBs: `chain_ait-hub…_chain.db.sql` → 4,711 blocks / 52 txs / `integrity_check ok`; `chain_coordinator.db.sql` → alembic `f53990f9d6cc`, 204 jobs, 166 tables, `integrity_check ok`.
- This closes the *file-level* half of the R10 restore-test gap: nightly dumps are restorable to valid databases. The service-level networked rejoin/restore test was completed post-close on `node0` (live hub snapshot via IDE bridge, rejoined at 5581 / `0xfab1122fe72f11`, `fleet-config-check.sh` exit 0); only the per-pull approval log residual remains.

## S-4 v3 activation armed at height 5470 (pending stamp verification)

- Fleet pulled to `38acca737` (v3-aware `compute_state_delta` with `extra_accounts` + per-escrow address — supersedes the v3-fallback guard from `f7262cc64`).
- `STATE_TRANSITION_V3_HEIGHT=5470` set uniformly in `blockchain.env` on all five hosts; coordinated stop-all/start-all of `aitbc-blockchain-node` (validators simultaneously, not rolling — the earlier rolling restart is what forked node1). Fleet reconverged at 5411→5413, identical hashes, no divergence.
- Blocks already self-stamp `state_transition_version` in metadata (v2 now) — v3 rules (per-escrow locks, job_id/provider requirements, beneficiary validation) apply from block 5470 on every node regardless of env, because `get_block_version` prefers the stamped metadata. Verify: `json_extract(block_metadata,'$.state_transition_version')=3` at height ≥5470.
- Monitor `/tmp/monitor_v3_activation.py` shows fleet converged at height **5453** and advancing with identical peak heights/hashes.
- **Activation verified 2026-09-08T16:52:18Z:** block **5470** reached on all five hosts with identical hash `0x7af8de9c1460a75e7a6f2b7e813d9a3628b9b7b56e0e3ac3c82d8be92788a7af` and `state_transition_version=3`.
- Follow-up check at 2026-09-08T16:53: all five active chain DBs at height **5471** also stamp `state_transition_version=3`; hashes identical.

## S-4 v3 `ESCROW_RELEASE`/`ESCROW_REFUND` pure path modeled (`1c8df3a50`)

- Commit `1df6eb365` (initial pure-path release/refund) and `1c8df3a50` (C901 refactor) on gitea `main`.
- `apps/blockchain-node/src/aitbc_chain/state/pure_state_transition.py`:
  - `StateDelta` gains `extra_debits` for per-account debits beyond sender/recipient.
  - `compute_state_delta` gains optional `escrow_context` (per-job `lock_version`, `expected_beneficiary`, `escrow_addr`).
  - v3 `ESCROW_RELEASE`/`ESCROW_REFUND`: validate `job_id`, bind recipient to `expected_beneficiary`, enforce `settings.escrow_settlement_authority`, debit only the fee from the sender, credit `value` to the recipient, and debit `value` from the per-escrow address via `extra_debits`.
  - v2 release/refund falls back to generic `value+fee` sender debit.
  - `apply_delta_to_map` and `apply_deltas_to_db` apply `extra_debits` and create missing accounts.
  - `extract_read_write_sets` includes the per-escrow address for release/refund dependency analysis.
- Tests added/updated in `apps/blockchain-node/tests/test_pure_state_transition_version.py`:
  - v3 release/refund success, wrong beneficiary, insufficient escrow, v2 fallback.
- `make lint-strict`, `make typecheck`, `make no-float-money` and focused blockchain-node tests passed on `node2`. Gitea CI run **19665** on `1c8df3a50` completed successfully.

## S-4 v3 parallel callers wired and v3 parallel re-enabled (`2baf883f3`/`fe5f4a451`/`e75e7355b`)

- `build_escrow_context` in `apps/blockchain-node/src/aitbc_chain/state/state_transition.py` prefetches per-job lock metadata (`lock_version`, `expected_beneficiary`, `escrow_addr`) for `ESCROW_RELEASE`/`ESCROW_REFUND` batches; returns `None` for missing locks or conflicting tx types.
- `apps/blockchain-node/src/aitbc_chain/consensus/poa.py` and `apps/blockchain-node/src/aitbc_chain/sync_block_import.py`:
  - build `escrow_context` for every v2/v3 parallel block;
  - pass it to `compute_state_delta`;
  - widen the parallel gate from `block_version == 2` to `block_version in (2, 3)`;
  - pre-fetch v3 per-escrow accounts so the pure path sees the actual locked balance.
- `apps/blockchain-node/tests/consensus/test_parallel_txs.py` updated to accept the new `escrow_context` keyword in the patched `compute_state_delta`.
- New test `apps/blockchain-node/tests/test_build_escrow_context.py` covers metadata prefetch, refund beneficiary, missing lock, non-escrow no-op, and conflicting release+refund.
- `make c901-ratchet`, `make typecheck`, `make lint-strict` and focused tests passed on `node2`. Gitea CI run **19668** on `e75e7355b` completed successfully.
- **Note:** `parallel_tx_validation` still defaults to `False`; the flag is now safe to enable across the v2/v3 boundary. Services were not restarted.

## Operator decisions + results (8 Sep, ~17:40)

- **Genesis key recovered**: `0xFe2d…9e03`'s private key exists on **hub only**, in four deprecated wallet files (`/var/lib/aitbc/wallets/genesis-real.json.deprecated`, `/var/lib/aitbc/wallets/genesis.json.deprecated`, `/root/.aitbc/wallets/genesis.json.deprecated`, `/root/.aitbc/wallets/genesis.json.pre-0x-migration`). Key material was never printed — files were scanned in-process by derive-and-compare. `GENESIS_PRIVATE_KEY` in `/etc/aitbc/blockchain-secrets.env` was corrected from the `0x2A9f…` key to the real `0xFe2d` key (verified by derived address), so `genesis_address`/key now agree and `execute_governance_proposal`'s self-signing path produces valid signatures again. `0xFe2d` is *not* a governance executor — operator chose **`governance_executors = 0xab07…` only**, so the endpoint path stays gated out regardless; the fix is config hygiene, not authorization.
- **DB-boot-level restore test (node0)**: the 20260908_010152 chain.db dump was restored to a scratch `AITBC_DATA_DIR` and booted through the app's own `init_db`/`session_scope` — clean open, `create_all`/column-migration ran without error, 4,708 blocks / 21 accounts / state roots intact. This was a *DB-boot-level* service verification.
- **Service-level networked rejoin/restore test (node0) — completed post-close**: a live online snapshot of `hub.aitbc`'s chain DB was taken with `sqlite3 ... .backup`, transferred to `node0` via the IDE host, installed as its chain DB, and `aitbc-blockchain-node` started. `node0` rejoined consensus and converged at fleet head 5581 / `0xfab1122fe72f11`; `fleet-config-check.sh` exit 0.
- **v3 activation**: kept at 5470 per operator decision — flip verification pending (~50 min from arming).
- **Deploy log (`a5b39ec666`)**: `update.sh` now appends one line per run to `/var/log/aitbc/deploys.log` (UTC ts, host, user, args, before→after SHA, exit code) via an EXIT trap — covers success, abort, and early exits. Closes the going-forward half of R10's per-pull evidence gap; gitea git history remains the authoritative trail.
- **Stale-data cleanup**: removed dead `devin-miner` + `e2e-test-miner` registrations from hub `coordinator.db` (7 completed jobs keep their `assigned_miner_id` history). Chain escrow table checked: all 58 rows terminal (48 refunded / 10 released) — no live orphans. Mesh topology confirmed intentional: node0 is a leaf follower (peers only to node1/node2, nothing peers *to* it) — not drift. Deprecated genesis wallet files purged post-close.

## Bridge validators registered + admin drift fixed (round 3)

- **Drift found & fixed**: `BRIDGE_ADMIN_ADDRESSES` had three different values across the fleet (`0xe738` hub/node1, `0xab07` node2, `0xFe2d` node0/hub2) — unified to `0xab0797…` in `blockchain.env` on all five; `aitbc-blockchain-rpc` restarted everywhere.
- **Bridge validators registered** on all five hosts' local `bridge_validators` tables: `0xab0797…` + `0x628b88c6…2C5F` (the dedicated key from `bridge-validator-keys.env`, present on hub/node1/node2), both active, epoch 0, `chain_id=ait-hub.aitbc.bubuit.net`. Self-signatures verified + admin-signed by `0xab07`. The 2-of-2 multisig threshold is now reachable — TASKLIST's `validator_count: 0` gap is closed.
- Signed on node2 and POSTed to each host's local RPC — signatures are portable; no key material left node2 or touched shell history.
- **Decisions recorded**: `aitbc_user` Postgres DSN rotation → **accepted risk** (dev fleet). v3 parallel validation → stays off until `ESCROW_RELEASE`/`REFUND` are modeled in the pure path (v3 blocks validate sequentially). Test rows in coordinator.db → **kept** as evidence.
- **Operator-blocked (round 3 only)**: node2's `tea` gitea token was expired (`invalid username, password or token`) — resolved later in the day (round 4 / final close).

## S-4 v3 ACTIVATED at block 5470 + parallel validation enabled (round 4)

- **Block 5470** (`0x7af8de9c…`) stamps `state_transition_version=3` — identical on all five hosts. The fleet crossed the v3 boundary cleanly; blocks continue past it (5471+).
- **`2baf883f3`** wires `escrow_context` into the parallel callers: new `build_escrow_context()` prefetches per-job lock metadata (lock_version, expected_beneficiary, escrow_addr) for ESCROW_RELEASE/REFUND batches, returning None on missing locks or conflicting release+refund so the block stays sequential. Gates widened `v2-only → v2+v3` in `poa.py` and `sync_block_import.py`. 5 new tests (`test_build_escrow_context.py`) cover prefetch/beneficiary/bail cases.
- **Operational keys deployed** to node0/hub2 (BOND_SLASH→validator-secrets.env, GENESIS_WALLET→node.env, bridge-validator-keys.env copied) — every host can now sign locally.
- Stale `0xFe2d` bridge-validator rows deleted fleet-wide; node2 `tea` login fixed by copying hub's working config.
- **`PARALLEL_TX_VALIDATION=true`** set fleet-uniform in blockchain.env, node services restarted — post-activation convergence verified at 5471.

## §17 verification remediations implemented (round 5, late)

The independent re-verification (MEGAPLAN §17) found the slash-authority env fix was incomplete — an inline `#` comment on the assignment made the value 110 bytes on 6 units. All S1–S7 done:

- **S1 (live)**: comment moved to its own line in `/etc/aitbc/blockchain.env` (node1, hub); 6 units restarted (node1 rpc+node; hub coordinator-api, rpc, bridge-monitor, node — sequenced to keep quorum). `/proc/<pid>/environ` verified len=42 on all.
- **S2+S3 (`9ce3489a2`)**: `canonical_address(strict=True)` raises on non-0x40hex at config boundaries; `bond_slashing.py` reads `BOND_SLASH_AUTHORITY_ADDRESS`/`BOND_BURN_ADDRESS` through it — malformed config now fails fast. 2 regression tests, suite green.
- **S4 verified**: BOND_SLASH mismatch paths already return named rejection reasons — the "bare return" claim was inaccurate; no change needed.
- **S5**: `fleet-config-check.sh` gained a `*_ADDRESS` shape check (`^0x[0-9a-fA-F]{40}$`) — passes post-fix.
- **S6**: the two stale lines struck above (162-run red streak; zero unmerged commits, branch gone).
- **S7**: chain-store path + the EnvironmentFile comment gotcha recorded in `AGENTS.md`.
- **SYNC_REDIS_URL password rotation — DONE post-close**: new `requirepass` set on `hub.aitbc`, `REDISCLI_AUTH`/`REDIS_URL`/`GOSSIP_BROADCAST_URL` and fleet `SYNC_REDIS_URL` updated, services restarted; verified with Redis `PING` and `fleet-config-check.sh` exit 0.

## Artifacts

- B-8 test: `apps/coordinator-api/tests/integration/test_b8_schema_conformance.py`
- 001_initial migration: `apps/coordinator-api/alembic/versions/001_initial_migration.py`
- mypy fix files:
  - `apps/coordinator-api/src/coordinator_api/contexts/advanced_rl/services/advanced_rl/engine.py`
  - `apps/coordinator-api/src/coordinator_api/contexts/multimodal/services/multi_modal_fusion/fusion_engine.py`

## node1 wedge recovered (round 6)

- Post-S1 restart, node1 held a stale prepared block at 5496 (`have=1` prepare votes, refusing canonical, no sync) — wedged at 5495 while fleet reached 5501. Operator-approved `aitbc-blockchain-node` restart cleared the in-memory prepare; `sync_bulk` imported 5496–5502 and node1 rejoined.
- **Fleet converged**: all five hosts at 5503 / `0x0d524dc2624a54` in a single height+hash sample.
- Methodology note (carried forward): "fleet converged" must assert all five heights AND hashes in one sample — a single-host reading can be a stalled head. Earlier "converged at 5495" was node1's stall, not the tip.

## S8 — chain-head convergence check added (dc1f1cf32)

`fleet-config-check.sh` now takes two height+hash samples across the fleet and flags only: (a) differing hashes at an equal height (fork), (b) a host trailing the fleet max by >2 blocks in both samples (lag), (c) `UNREACHABLE` probes — kept distinct from divergence after two transient empty-sweep false alarms today. Live run verified: all five at 5506/`0xa26d7a23` in both samples. Residual informational drift: `BOND_BURN_ADDRESS` unset on node0 (leaf follower; code default covers it).

## Deployment script fixes (`596d73844`, `04fbbcc8a`, `976cec9e9`)

- `install-profiles.sh` / `aitbc-backup.service` (`596d73844`, `04fbbcc8a`) — see above.
- `update.sh` `get_node_role()` (`976cec9e9`):
  - Reads both UPPER_CASE and lower_case profile keys (`BLOCKCHAIN_MODE`/`MARKET_ROLE`/`HARDWARE_PROFILE`) from `blockchain.env` and `node.env`, fixing nodes where lower-case keys were ignored.
  - When `HARDWARE_PROFILE` is unset, defaults to the detected hardware instead of treating it as `nogpu` and warning about an override.
  - Verified on `node0`: `update.sh --no-pull --no-restart --no-migrate` prints `GPU confirmed: NVIDIA GeForce RTX 4060 Ti` instead of the previous `⚠ GPU detected ... overriding to gpu` warning.
  - `node0` `BOND_BURN_ADDRESS` set to the fleet value `0xf692dddeffe395394ac4f444504a1bdad95cf123`; `fleet-config-check.sh` exit is now green when the fleet converges.

- Warning reproduced: `install-profiles.sh` failed because the Poetry venv was on 1.8.3, which cannot read `pyproject.toml` `[project]` package-mode metadata, and because `sqlcipher3-binary 0.6.0` has no aarch64 wheels.
- `scripts/deployment/install-profiles.sh` (already in `dc1f1cf32`) now upgrades Poetry to `>=2.4.1,<3` unconditionally, so the `[project]` block is parsed.
- `pyproject.toml`/`poetry.lock` (`596d73844`) add `platform_machine == 'x86_64'` marker to the optional `sqlcipher3-binary` dependency so `poetry export` omits it on ARM hosts.
- `scripts/deployment/update.sh` (`04fbbcc8a`) links systemd units on demand in Step 0 if `aitbc-backup.service` is not present, instead of warning and skipping the pre-update backup.
- Verified on `hub.aitbc` (aarch64): removed the `aitbc-backup.service` symlink, ran `update.sh --no-pull --no-restart --no-migrate`, and Step 0 linked the unit, ran the oneshot backup, completed `install-profiles.sh customer-no-gpu` with no fallback, and finished successfully.

## Close-out corrections (round 7)

- **CI on the tip is verified green**: Actions run **19669** on `9ce3489a2` (S1–S7) = `success` (19:36→19:48). Run **19670** on the S8 tip `dc1f1cf32` was `running` at last check — the actions API (`/repos/oib/aitbc/actions/tasks`) works from node2's `aitbc-node2` login; the earlier "not found" was the dead-token path, not a missing endpoint.
- S8 is **built and on main** (`dc1f1cf32`), not merely specced — the earlier close-out read `9ce3489a2` as tip before the push landed.
- node2 `tea` login is **resolved** (fresh `aitbc-node2` PAT + hub's config copy; `tea whoami` and `tea api` both authenticate).
- Operator lane and R10 networked rejoin/restore test completed post-close: `SYNC_REDIS_URL`
  password rotated, deprecated `genesis` wallet / key-bearing `.bak` / backup archives purged,
  node2 `tea` token verified, and `node0` restored from a live hub snapshot and rejoined
  consensus at 5581 / `0xfab1122fe72f11`. Remaining: per-pull approval log (not establishable,
  not manufactured).

## F1/F2 + timer decision (round 8)

- **F1 fixed** (`976cec9e9`): `fleet-config-check.sh` auto-probes candidate addresses per canonical host (ssh aliases on the IDE, FQDNs/shortnames on fleet nodes) — runs wherever ssh reach exists.
- **F2 fixed**: `BOND_BURN_ADDRESS` set on node0 to the fleet value — the permanent-red exit is gone; first fully green run: all five at 5527/`0xf635f392`, hub2's +1-block skew correctly tolerated.
- **Timer decision**: dropped. Fleet nodes have no inter-node ssh — and that's not a security posture to preserve or weaken: **ssh is dev-tier only, not part of the AITBC software design**. Nodes communicate over their own gossip/RPC protocols; shell access is an operator-side convenience from the IDE host. The check therefore stays a manual/IDE-side dev tool; units removed from node2, not committed.

## F1 done right — pull-based convergence over node RPC (aef0b5099)

- The previous F1 fix renamed host candidates but couldn't work: fleet nodes have no inter-node ssh *by design* (ssh is dev-tier, not part of the software). The convergence section now polls each node's own `/rpc/status` (`height`+`last_block_hash`) — the surface peers already expose — so it runs from **any** host with network reach. Verified from node2: all five heads at 5547/`0x3df929cc`, exit 0. Env-drift sections auto-skip when no ssh reach exists (they're inherently IDE-side).
- **CI green through the tip chain**: runs 19669–19672 all `success` on `9ce3489a2`→`976cec9e9`; run **19673** on `aef0b5099` is green (verified independently from the IDE host).
- **Final close**: F1/F2 closed in `aef0b5099`; §14 acceptance re-asserted at `aef0b5099` / CI 19673 green. R10 residual recorded: per-pull approval log not establishable / not manufactured; service-level networked rejoin/restore test completed on `node0` (live hub snapshot, rejoined at 5581 / `0xfab1122fe72f11`, `fleet-config-check.sh` exit 0). TASKLIST reconciled. Operator lane and R10 restore test completed post-close.

## Post-close active outage: chain down at 5581 (22:49 → 23:28)

- **Symptom**: chain frozen at 5581 (`0xfab1122fe72f118c`), all five hosts, `delta=0` over 70s. Hub `aitbc-blockchain-node` crash loop 415 restarts.
- **First finding**: Redis URLs were missing the auth-colon (`redis://PASSWORD@host` instead of `redis://:PASSWORD@host`). Fixed on all hosts.
- **Second finding**: the fix restored Redis auth but the proposer still could not gossip because `blockchain.env` forced `GOSSIP_BACKEND=websocket` with `GOSSIP_WEBSOCKET_URL=wss://hub.aitbc.bubuit.net:443`. On the hub host that hostname resolves to the hub itself; no 443 listener exists locally, so every proposer attempt failed.
- **Fix**: switched the fleet to `GOSSIP_BACKEND=mesh` in `aitbc-blockchain-node.env`, with `GOSSIP_MESH_PEER_URLS=wss://<peer>/rpc/gossip/ws` and a local `GOSSIP_BROADCAST_URL`. Commented the `GOSSIP_*` overrides in `blockchain.env`.
- **Redis password rotation**: the password was exposed in debugging output; it was rotated again fleet-wide and the old value invalidated.
- **Verification**: `fleet-config-check.sh` now samples twice 60s apart and requires the fleet max height to increase; last run 5591 → 5592, all five hosts same hash, exit 0. `MEGAPLAN.md` §17.15/§17.16 and `TASKLIST.md` updated.
