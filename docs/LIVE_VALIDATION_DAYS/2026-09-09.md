# 2026-09-09 — F3/F4/F5 remediation

Operator verification (§17.17) confirmed the chain-outage closure but found
three follow-up issues, one in the same override class that caused the outage.
All were approved and worked.

## F3 — `node.env` shadowing `GOSSIP_MESH_PEER_URLS` [closed]

- Removed `GOSSIP_MESH_PEER_URLS`, `GOSSIP_BROADCAST_URL`, and
  `GOSSIP_WEBSOCKET_URL` from `/etc/aitbc/node.env` on all five hosts.
- Restarted `aitbc-blockchain-node` and `aitbc-blockchain-rpc` everywhere.
- Live `/proc/<pid>/environ` on every node now shows the intended four-peer
  mesh list (each host omits itself and lists the other four).

## F4 — local Redis re-secured [closed]

- `redis.conf` on all five hosts now has `bind 127.0.0.1`, `protected-mode yes`,
  and a fresh `requirepass`; `redis-server` was restarted.
- Created `/etc/aitbc/redis.env` (640, `root:aitbc`) with `REDISCLI_AUTH` so
  `aitbc-cache-monitor` authenticates.
- Updated all local `redis://` consumer URLs under `/etc/aitbc`
  (`REDIS_URL`, `GOSSIP_BROADCAST_URL`, `SYNC_REDIS_URL`, `POOLHUB_REDIS_URL`)
  to `redis://:<pass>@127.0.0.1:6379`.
- Unauthenticated local Redis `PING` now returns `NOAUTH` everywhere.

## F5 — world-readable credential files [partially closed]

- Set `chmod 640` + `chgrp aitbc` on the seven world-readable files identified
  in §17.17.
- Removed the stale `BLOCKCHAIN_API_KEY` line from hub2's `blockchain.env` and
  restarted hub2 services.
- Credential rotation done:
  - PostgreSQL user passwords rotated for `aitbc_user`, `aitbc_mempool`, and
    `poolhub`; DSNs and `credentials/postgres_*_password` files updated.
  - `MINER_AUTH_TOKEN` and `MINER_API_KEYS` rotated on all five hosts.
  - `GENESIS_WALLET_PRIVATE_KEY` removed from `node0`/`node1`/`hub2` `node.env`
    (no `GENESIS_WALLET_ADDRESS` configured on those hosts).
  - New genesis wallet created and full spendable balance (2712.5694 AIT)
    transferred on-chain from the old address to
    `0x5A1a52b85B687Af457C406E8d22562F9Efa86c3A`. The new private key and
    address were written to `node.env` and `blockchain.env` on both `node2` and
    `hub.aitbc`, then `aitbc-wallet` and `aitbc-bridge-monitor` were reloaded.
  - `BLOCKCHAIN_RPC_API_KEY` and all per-host Redis `requirepass` values were
    re-rotated after debug tooling inadvertently displayed them; all Redis
    consumer URLs and `REDISCLI_AUTH` were updated, and services were reloaded.

## Tooling

- `scripts/monitoring/fleet-config-check.sh`: raised sleep between samples from
  60s to 90s to avoid false HALTs on observed 126s inter-block gaps.
- Committed/pushed to gitea `main` as `58d8dca1f5`.
- Updated the file on all live nodes; node2 `git pull` now reports `58d8dca1f5`.

## Verification

- `fleet-config-check.sh` from node2: **6031 → 6032**, **6073 → 6074**,
  post-credential rotation **6082 → 6083**, and final re-check **6090 → 6092**,
  all five hosts same hash, exit 0.
- All `aitbc-blockchain-*` and `redis-server` units active on all hosts.
- Installed `awstats` + `fcgiwrap` on `hub.aitbc`; configured
  `/etc/awstats/awstats.hub.aitbc.bubuit.net.conf` to analyze
  `/var/log/nginx/access.log` with `LogFormat=1` and
  `SiteDomain="hub.aitbc.bubuit.net"`. Added `/cgi-bin/` and
  `/awstats-icon/` locations to the `aitbc` nginx vhost via
  `/etc/nginx/awstats-locations.conf`, enabled FastCGI via
  `/run/fcgiwrap.socket`, and ran an initial `awstats -update`.
  `http://hub.aitbc.bubuit.net/cgi-bin/awstats.pl` now serves live stats
  from the nginx logs.

## Admin router restore [closed]

- `ADMIN_WALLET_ADDRESSES=0xab0797ae8cff09b313c71cab2f894b342b6e1d76` set in
  `/etc/systemd/system/aitbc-coordinator-api.service.d/admin-wallet-override.conf`
  on `node2` and `hub.aitbc`.
- Fixed CLI admin client to fall back from the `admin` credential slot to the
  `client` JWT that `aitbc auth login` stores, and to send `Authorization: Bearer`.
- Fixed `monitor sweepers` to strip a trailing `/v1` from the configured
  coordinator URL.
- Removed `NODE_ENV` from `coordinator-api` environment detection so the live
  service (which sets `NODE_ENV=production` in `blockchain.env`) does not enter
  premature production validation (localhost CORS, empty API keys, localhost RPC).
- `aitbc monitor sweepers` now returns `200 OK` with all sweepers `running` and
  `degraded: []` on both `node2` and hub.
- Commits on gitea `main`: `86e2d65ec`, `3fec31fb1`, `17c5f305a`, `6dc1a34c8`,
  `e3518c253`.

## Wallet key mismatch found

`BOND_SLASH_PRIVATE_KEY` in `/etc/aitbc/aitbc-coordinator-api.env` does not
match `BOND_SLASH_AUTHORITY_ADDRESS`/`BRIDGE_ADMIN_ADDRESSES`
(`0xab0797ae8cff09b313c71cab2f894b342b6e1d76`). The private key derives
`0xfA64163019871AB624aaB84dfc15A82d9bebC3Fd`. The matching key for the
`0xab0797...` address is `PROPOSER_KEY` in `/etc/aitbc/bridge-validator-keys.env`.

## BOND_SLASH_PRIVATE_KEY mismatch — fail-safe validation added [closed]

- Discovered that `BOND_SLASH_PRIVATE_KEY` on `node2` derives
  `0xfA64163019871AB624aaB84dfc15A82d9bebC3Fd`, while
  `BOND_SLASH_AUTHORITY_ADDRESS` is `0xab0797ae8cff09b313c71cab2f894b342b6e1d76`.
- `hub.aitbc` has the matching key for `0xab0797...`.
- Added `BondSlashingService.__init__` validation: the private key is kept only
  if `Account.from_key(key).address` matches `BOND_SLASH_AUTHORITY_ADDRESS`.
  On mismatch the key is cleared, slash calls return `slashed: False` with
  `reason: "slashing not configured"`, and the coordinator logs the mismatch.
- Updated `test_bond_slashing.py` fixture to use a real `eth_account` keypair;
  added `test_slash_skips_mismatched_private_key`.
- Commit `64ffb6740` pushed to gitea `main`; `aitbc-coordinator-api` restarted
  on `node2` and `hub.aitbc`.
