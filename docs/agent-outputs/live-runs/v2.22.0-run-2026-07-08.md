# v2.22.0 Live-Run — 2026-07-08 (pausiert: Wochenlimit)

**Harness:** `~/boilerplate-rc-dogfood` @ `v2.22.0-rc1` (main `435516d` + generate-governor) ·
**Target:** dev repo · **Fence:** `key in (ABS-138, ABS-152, ABS-153) OR parent in (…)`
**Launcher:** Session-Scratchpad `launch-orchestrator-v2220.sh` (Rezept unten)

## Ergebnis bis zur Pause (07:33–08:33Z, ~$14 über ~25 Seats)

| Epic | Stand |
|---|---|
| ABS-138 (Audit, 8 Stories) | **Enrichment** — Path-B lief voll automatisch: Intake → PO Triage → Grooming (bsa) → Enrichment; Runner wandte Handoff-Transitionen selbst an (ABS-132/137 live bestätigt) |
| ABS-152 (Atlassian MCP) | **Stories In Flight**, 5 Kinder ABS-158..162 erstellt (Operator, s. Vorfall 2), alle „Selected for Development" |
| ABS-153 (Runner-Fixes) | **Stories In Flight**, ABS-154 in Dev, ABS-155..157 depends-on:ABS-154 |

Angefangene, uncommittete Seat-Arbeit liegt in `tmp/ABS-154|158|161-work` (Session-Resume setzt dort auf).

## Vorfälle

1. **TRACKER_CMD-Pfad (Operator-Fehler, behoben):** Run #1 mit `TRACKER_CMD=$HARNESS/scripts/jira-tracker.sh`
   → Seat-Sandbox verweigert den externen Pfad; die Target-Allowlist erlaubt nur `scripts/jira-tracker.sh`
   (relativ) bzw. den absoluten **Target**-Pfad. Fix: `TRACKER_CMD=$TARGET/scripts/jira-tracker.sh`, Neustart,
   stale Session-Pointer löschen (sonst Resume mit kaputtem Packet).
2. **Grooming-Seat ohne Write (ABS-152, Operator-Übernahme):** Der v3.0-Grooming-po-agent (empty-epic,
   cwd = Main-Checkout) braucht Write für `work/scratch/`-Bodies + Compound-Bash → von der restriktiven
   Main-Allowlist blockiert (Path-B-Seats mit reinen Tracker-Calls kamen durch). Der Seat lieferte statt
   Loop einen kompletten Ausführungsblock (ABS-137-Truthfulness) → Operator führte ihn aus (5 Kinder).
   **Befund-Kandidat Iteration 5:** Main-Checkout-Allowlist um `Write(work/scratch/**)` erweitern.
3. **API-Ausfall 08:10Z (transient):** „Unable to connect to API (ConnectionRefused)" killte alle 5
   Implementer-Seats gleichzeitig → Retry-Packets mit **leerer TICKET-Sektion** (auch der Packet-Build-
   `tracker get` schlug fehl; die Retry-Seats zogen korrekt Stop-the-Line). Selbstheilung via Backoff +
   Stale-Lock-Reclaim (ABS-150 live bestätigt). **Befund-Kandidat:** Packet-Build sollte bei leerem
   Ticket-Dump abbrechen statt einen leeren Packet zu spawnen.
4. **Wochenlimit erschöpft (Pause-Grund):** „You've hit your weekly limit · resets Jul 11 at 6am
   (Europe/Vienna)". Alle Spawns crashen sofort; Runner via Stop-File sauber beendet (Exit 0, drained).
   ABS-159/160/162 + ABS-138(issue-enrichment) trugen beim Stop 2–3 Crash-Marker → beim Restart droht
   sofortige CRASH-LIMIT-Eskalation zu Needs PO Decision (po-agent-Decision-Seat löst das, oder Marker
   vorab per Kommentar/Handoff neutralisieren).

## Restart-Rezept (nach Fr 11.07. 06:00 Wien)

1. `rm -f work/.orchestrator-stop`
2. Launcher erneut ausführen (identische Env; `TRACKER_CMD` = Target-Pfad!)
3. Erwartung: Session-Resume auf ABS-154/158/161; Needs-PO-Decision-Eskalationen der Crash-Marker-Tickets
   werden von po-agent-Decision-Seats behandelt — beobachten.
4. Bewährte Env-Workarounds (ABS-153 fixt sie dauerhaft): `ORCH_WORKTREE_EXTRA_ALLOW` mit bare
   Bash/Write/Edit · `ORCH_MAX_TURNS=35` / `_BE_DEVELOPER=50` · `ORCH_AGENT_TIMEOUT=2400` ·
   **Achtung:** `ORCH_LOCK_TTL` (1800) < Timeout (2400) → Reclaim kann laufenden Seats das Lock nehmen;
   beim Restart `ORCH_LOCK_TTL=3000` setzen.
