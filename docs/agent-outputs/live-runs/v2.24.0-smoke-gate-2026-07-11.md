# v2.24.0 Release-Gate: Live Behavioral Smoke-Run

**Date**: 2026-07-11 · **Gate**: PRE-RELEASE-CHECKLIST §4 — Agent-Defs geändert (qas [ABS-209], qas-design [ABS-192], _common-rules [ABS-195], issue-enrichment [ABS-203]).

## Setup

- **Harness**: Dev-Repo @ Release-Stand `a725dab` — Defs served aus `harness/claude/` (verifiziert via `config_agents_dir`-Auflösung).
- **Target**: isolierter Scratch-Clone `/private/tmp/v2240-smoke/target`, Mock-Tracker, `ORCH_MAX_CONCURRENT=1`, DEMO-1-Fixture vorab entfernt (Lektion v2.23.0-Gate).
- **Story**: DEMO-2 „SMOKE-1: append v2.24.0 README marker line" (be-developer, trivial).

## Ergebnis: PASS (mit 1 Befund)

| Kriterium (§4) | Befund |
|---|---|
| Gate-Verdikt-Qualität | Volle Kette: be-developer → system-architect (In Review, APPROVED) → qas (In Test, PASSED, QA-Report committet) → po-agent-Acceptance-Check + Human-NOTIFY → Rast am Human-Gate (`Ready for Human Acceptance`). Deliverable `386eee3` + QA-Report `eb802f8` auf `DEMO-2-auto`, Diff exakt 2 Dateien. |
| Rework-Bounces | **0** STATION-GUARD / **0** SPAWN-CRASH / **0** RESPAWN — Baseline v2.23.0 hatte hier noch den qas-Done-Skip-Guard-Bounce. |
| ABS-209-Wirkung | **Kein `In Test → Done`-Skip mehr** (die 10×-Klasse des v2.23.0-Runs). AC3-Restverifikation (≥3 qas-Visits) läuft im nächsten Live-Run mit. |
| Kosten (run.log) | be-dev **$0.78**, SA **$0.55** (opus), qas **$0.48**, po **$0.52** — auf v2.23.0-Smoke-Niveau ($0.49/$0.32/$0.23 + po), ABS-195/207-Mehrlogik kostet nichts Messbares. |
| ABS-207/194-Diagnostik | `SEAT-CWD`-Zeilen pro Spawn sichtbar; qas lief worktree-isoliert (`tmp/DEMO-2-work`). |

## Befund (Follow-up, kein Blocker)

Der qas transitionierte `In Test → Ready for Human Acceptance` statt (per Def, ABS-209) `→ Story Acceptance`. Der STATION-GUARD ist dafür **blind**: „Ready for Human Acceptance" hat keinen `chain_index` (Out-of-Chain-Status), `first_skipped_mandatory` evaluiert nicht. Funktional lief der PO-Acceptance-Check trotzdem (der Sweep mappt RfHA → `SPAWN-NOTIFY po-agent`), das Human-Gate wurde korrekt erreicht — aber die Story-Acceptance-Station wurde unbemerkt gefaltet. → Ticket: Guard-Chain-Awareness für Out-of-Chain-Stati + qas-Def-Nachschärfung.

**Verdikt: Release-Gate bestanden — v2.24.0 promotionsfähig.**
