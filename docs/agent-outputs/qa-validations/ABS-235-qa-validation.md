# QA Validation Report — ABS-235

**Ticket**: ABS-235 — Backend S3: Entity-Ops + kanonischer Renderer
**Branch**: `ABS-235-auto` · **Commit validated (resume)**: `722dae7`
**QAS actor**: qas · **Initial pass**: 2026-07-15 (commit `a377c96`, 82/82) · **Resume pass**: 2026-07-15 (commit `722dae7`, 102/102)
**Verdict**: ✅ **APPROVED**

---

## Resume Context

This is a **resume validation** after the system-architect's two post-QA fixes:

| Commit | Change | Scope |
|---|---|---|
| `17ead95` | Schema prefix `routes_test_` → `item_routes_test_` to avoid S4 collision | Test-only (items-routes.test.ts) |
| `722dae7` | `--test-concurrency=1` in both packages' test scripts (prevents OID-race DROP SCHEMA) | Test-only (package.json scripts) |

**Product code is unchanged.** All AC/DoD findings from the initial pass remain valid.

The test count is now 102 (vs 82 at initial pass) because ABS-236 (S4: comments/transitions/events/SSE) merged onto the epic branch during the architect review window — its 20 tests now run alongside the S3 suite. All 102 pass.

---

## Validation Environment

- PostgreSQL 16-alpine (Docker container `backend-db-1`, port 5432 — fresh start for QA run)
- `DATABASE_URL=postgres://postgres:postgres@localhost:5432/agentic`
- `pnpm typecheck && pnpm lint && DATABASE_URL=... pnpm test` run from `backend/` in worktree `/tmp/ABS-235-work`
- `--test-concurrency=1` isolates schema creates/drops across all test files
- Schema-isolated per test suite (`item_routes_test_<ts>`, `events_routes_test_<ts>`, etc.)

---

## Test Results

### Initial Pass (commit `a377c96`, 2026-07-15)

| Suite | Total | Pass | Fail |
|---|---|---|---|
| `packages/core` | 69 | **69** | 0 |
| `apps/server` | 13 | **13** | 0 |
| **TOTAL** | **82** | **82** | **0** |

### Resume Pass (commit `722dae7`, 2026-07-15)

| Suite | Total | Pass | Fail |
|---|---|---|---|
| `packages/core` | 80 | **80** | 0 |
| `apps/server` | 22 | **22** | 0 |
| **TOTAL** | **102** | **102** | **0** |

```
packages/core test: ℹ pass 80 / fail 0 / skipped 0 / duration_ms 1822
apps/server test:   ℹ pass 22 / fail 0 / skipped 0 / duration_ms 2208
```

Delta: +20 tests = S4 (ABS-236) suite running co-resident; all pass.

**TypeScript typecheck**: `pnpm typecheck` → PASS (both packages, zero errors)
**Lint**: `pnpm lint` → PASS (clean, zero warnings)

---

## Acceptance Criteria — Verification Matrix

### AC#1 — Byte-identical `get` + terse mock line formats for search/children/parent/child-count

**Result**: ✅ PASS

Evidence (test names, resume run):
- `AC#1: get EP-1 is byte-identical to the mock fixture` ✓
- `AC#1: get DEMO-1 is byte-identical to the mock fixture` ✓
- `AC#1: get DEMO-2 is byte-identical to the mock fixture` ✓ (rich fixture: optional fields, 10 comment kinds, umlauts/emoji, orchestrator-ready projection)
- `AC#1: get DEMO-3 is byte-identical to the mock fixture` ✓ (child ticket with parent key)
- `AC#1: get DEMO-4 is byte-identical to the mock fixture` ✓
- `AC#1: unicode/umlaut/emoji round-trip through the body and comments` ✓
- `AC#1: children/parent/child-count match the mock byte formats` ✓
- `terse ops carry the mock's trailing newline (children/parent/child-count/search)` ✓ (route-level)

Golden fixtures live at `tests/fixtures/tickets/` (EP-1, DEMO-1..4), generated by `scripts/mock-tracker.sh` (authentic mock bytes, not hand-crafted). Test uses full-string equality — equivalent to `diff <(mock get X) <(backend get X) == empty`.

DEMO-2 (1 066 bytes) covers every optional frontmatter field, all 10 comment kinds, unicode/emoji, and comment bodies carrying their own `### sub-headings`.

### AC#2 — Error mapping: 404 "no such ticket", 400 status-via-update

**Result**: ✅ PASS

Evidence:
- Core: `AC#2: unknown item → 404 'no such ticket'` ✓ — exact string `"no such ticket: NOPE-9"`
- Core: `AC#2: status via update → 400 with the mock's exact message` ✓ — exact string `"update: status changes must go through 'transition' (validated + reasoned)"`
- Routes: `unknown item → 404, plain body 'no such ticket'` ✓ — HTTP 404, `content-type: text/plain`
- Routes: `status via update → 400 with the mock's message` ✓ — HTTP 400

Per spec §4 / ABS-313 (21): the mock-identical stderr text is the server body (`text/plain`), printed verbatim by the S5 adapter shim.

### AC#3 — fields jsonb validated against field_schema; body stored verbatim

**Result**: ✅ PASS

Evidence:
- `fields.ts`: custom JSON-Schema-subset validator (no ajv, per spec §1 dependency budget)
- `validateItemFields()` resolves `entity_type.field_schema` and validates; unseeded types pass through
- `body` stored verbatim — never parsed into columns
- Round-trip byte-identity for all 5 golden fixtures (AC#1) confirms body-verbatim storage

### AC#4 — Every write creates revision snapshot + event

**Result**: ✅ PASS

Evidence:
- `AC#4: create/update/link/assign each snapshot a revision and emit an event` ✓ (4 ops, 4 events, 4 revision rows)
- `snapshotAndEvent()` runs in ONE transaction for all write ops
- `AC#4: fault injection — a failing event insert commits NO part` ✓ (from S2 suite, co-resident)
- `link` idempotent replay: 0 extra events (verified separately)
- `assign` with empty accountId: graceful no-op, 0 events

### AC#5 — Timeline projection: mixed v2 comments + backend-native transitions

**Result**: ✅ PASS

Evidence:
- `AC#5: imported comments and native transition events merge chronologically, byte-identical` ✓

Test procedure:
1. Imports a ticket with 2 comment rows at T1 and T3
2. Inserts a backend-native transition EVENT at T2 (no comment row, per [A-313] §3)
3. Asserts `getItem()` output is byte-identical to the expected mixed-timeline string

Transition event renders as `kind: transition-reason` block with body `Transition: <from> -> <to>. Reason: <reason>` — byte-identical to the mock's format. Same-second ties: comment rows before events (deterministic).

### AC#6 — orchestration_state ↔ label projection

**Result**: ✅ PASS

Evidence:
- `AC#6: the orchestrator-ready label maps to orchestration_state and lives in NO table` ✓
  — `labels[] = []` (no stored label row), `orchestration_state = 'eligible'`, audit event recorded, `get` shows `labels: [orchestrator-ready]`, `search ?label=orchestrator-ready` matches
- `labels projection keeps other labels and puts orchestrator-ready first` ✓
  — `labels: [orchestrator-ready, batch-candidate]` rendered correctly; stored labels exclude the token
- DEMO-2 golden fixture contains `orchestrator-ready` → passes AC#1 (end-to-end projection verified)
- Migration adds `orchestration_state text NOT NULL DEFAULT 'excluded'` with 4-value CHECK + GIN index

### AC#7 — search completeness: 1000+ tickets all returned

**Result**: ✅ PASS

Evidence:
- `AC#7: search is complete — a 1000+ ticket fixture returns every row` ✓ — 1 200 rows inserted, `lines.length === 1200` asserted
- No `LIMIT` clause in `searchItems()` — complete by contract per [A-313] / ABS-313 (9)
- Search order: `priority ASC, created ASC, key ASC` (PostgreSQL enum declaration order `hotfix|high|normal|low` → ASC = priority DESC effectively = operator SCOPE-APPEND satisfied)

### Security AC (folded from ABS-233 review) — Cross-tenant org-wide token isolation

**Result**: ✅ PASS

Evidence:
- `org-wide token cannot authenticate against a project in another org → 403` ✓ (org-A token → org-B project `ONLYB` → HTTP 403)
- `org-wide token addressing a same-KEY project resolves its OWN org's project` ✓ (org-A token sees own project's data, org-B's `SECRET-1` ticket is unreachable → 404)

Fix in `auth.ts`: `SELECT id FROM project WHERE key = $1 AND org_id = $2` — always resolves inside the token's own org. `targetProjectId` is the only project id that routes use. Test has teeth (negative control verified by be-developer: restoring old guard causes AuthZ test to fail).

### Operator SCOPE-APPEND — Priority field

**Result**: ✅ PASS

Evidence:
- `search orders priority DESC then age ASC (operator SCOPE-APPEND)` ✓
- `AC#5: priority defaults to 'normal' when unset` ✓
- `AC#5: every operator-specified priority value is accepted` ✓ (`hotfix|high|normal|low`)
- `AC#5: an invalid priority is rejected by the enum` ✓
- Renderer: `priority:` emitted ONLY when `!= 'normal'` — endorsed judgment call: the mock has no priority field, so always emitting breaks byte-identity on all existing fixtures. DEMO-2 passes AC#1, confirming the judgment is correct.

---

## Definition of Done — Verification

| DoD Item | Status |
|---|---|
| Golden-File-Suite + Routen-Tests grün | ✅ 102/102 PASS (80 core + 22 server) |
| Fixtures unter tests/fixtures geteilt mit der Mock-Suite | ✅ `tests/fixtures/tickets/` — EP-1, DEMO-1..4 generated by `scripts/mock-tracker.sh` |
| TypeScript typecheck clean | ✅ PASS (both packages) |
| Lint clean | ✅ PASS |
| Commit pushed on branch | ✅ `722dae7` on `origin/ABS-235-auto` |

---

## Summary of Architect Fixes (resume delta)

The two commits applied by the system-architect after the initial QA pass are **test-infrastructure only**:

1. **`17ead95`** — Changed schema prefix in `items-routes.test.ts` from `routes_test_${Date.now()}` to `item_routes_test_${Date.now()}` to prevent collision with S4's events test schema when both load within the same millisecond.
2. **`722dae7`** — Added `--test-concurrency=1` to both `packages/core` and `apps/server` test scripts to serialize schema creation/drop across test files, eliminating an OID-race that caused ~33% flakiness when running in parallel.

No product code was touched. All AC/DoD findings from the initial pass remain fully valid.

---

## Final Verdict

All 7 AC items, the security AC, and the operator SCOPE-APPEND are **verified and PASSING** through the test suite run against a real PostgreSQL 16 instance. Zero failures. TypeScript typecheck and ESLint are clean. The golden-file byte-identity contract is genuine (full-string equality against mock-tracker-generated fixtures). The cross-tenant security fix is verified with both a positive and a negative control.

**Final verdict (resume): APPROVED — proceed to Story Acceptance.**
