# v0.5.11 Change Log

**Theme**: Type safety hardening — systematic MyPy error elimination across the `aitbc/` shared core library, duplicate route audit, and systemd symlink repair.

**Goal**: Achieve 0 MyPy errors across all non-excluded files in `aitbc/`, fix all Ruff lint issues, migrate deprecated `TypeAlias` syntax, remove duplicate route registrations in coordinator-api, and repair the broken `aitbc-recovery` systemd symlink.

**Context**: v0.5.10 implemented the compute-seconds unit system (1 AIT = 3600 seconds). v0.5.11 focuses on code quality and type safety: eliminating implicit Optional bugs, unreachable code, incorrect attribute access on `None`-typed variables, redundant `.json()` calls on already-parsed dicts, and missing type annotations across 20+ files in the shared core library.

**Status**: Fully implemented. All 17 planned items are complete.

**Post-release work (v0.5.12 Agent B)**: 8 additional commits extending v0.5.11 with database performance indexes, config consolidation, dead code removal, and large-file decomposition. See "Post-Release Changes" section below.

---

## Implementation Status

| # | Task | Priority | Status |
|---|---|---|---|
| 1 | Fix MyPy errors in `aitbc/queues/` — implicit Optional, call-overload, unreachable | High | ✅ DONE |
| 2 | Fix `AITBCHTTPClient` response typing in `blockchain_service.py` — `.json()` on dict | High | ✅ DONE |
| 3 | Fix MyPy errors in `aitbc/database/` — `connection.py` and `replica.py` | High | ✅ DONE |
| 4 | Fix MyPy errors in `distributed_tracing.py` — import stubs, `TracerProvider` | High | ✅ DONE |
| 5 | Fix MyPy errors in `agent_bridge/src/integration_layer.py` — `union-attr`, `no-any-return` | High | ✅ DONE |
| 6 | Fix MyPy errors in `api_utils.py` — implicit Optional | High | ✅ DONE |
| 7 | Fix MyPy errors in `tracing.py` — import stubs, `attr-defined` | High | ✅ DONE |
| 8 | Fix MyPy errors in `agent_trading` + `agent_compliance` — `unused-ignore`, `no-any-return` | High | ✅ DONE |
| 9 | Fix MyPy errors in `ethereum_rpc.py`, `access_control.py`, `price_oracle.py` | High | ✅ DONE |
| 10 | Fix MyPy errors in `agent_registry/src/discovery.py` — implicit Optional | High | ✅ DONE |
| 11 | Fix MyPy errors in `config/__init__.py` + `hierarchical_config.py` | High | ✅ DONE |
| 12 | Fix MyPy errors in `agent_registry/tests/` — `import-not-found` | High | ✅ DONE |
| 13 | Fix MyPy errors in `agent_protocols/`, `events/`, `circuit_breaker/`, `stage_runner/`, `registration.py` | High | ✅ DONE |
| 14 | Fix Ruff errors | Medium | ✅ DONE (none found) |
| 15 | Migrate deprecated `TypeAlias` syntax in `config/__init__.py` (UP040) | Medium | ✅ DONE |
| 16 | Audit duplicate route registrations in coordinator-api | Medium | ✅ DONE |
| 17 | Fix circuit breaker unreachable code + state manager unreachable statements | Low | ✅ DONE |
| 18 | Fix `aitbc-recovery` broken symlink | Low | ✅ DONE |
| 19 | Clean up stale `type: ignore` comments flagged by `--warn-unused-ignores` | Medium | ✅ DONE |
| 20 | **Bug:** Broken `AgentServiceBridge` import in trading + compliance agents (`ModuleNotFoundError`) | High | ✅ DONE |
| 21 | **Bug:** Biased/bursty read-replica routing using `hash(time.time())` as PRNG | Medium | ✅ DONE |
| 22 | **Bug:** `debounce` decorator propagated `CancelledError` to superseded callers | Medium | ✅ DONE |
| 23 | **Bug:** PoA block proposer recorded `nonce - 1` for confirmed transactions (off-by-one) | High | ✅ DONE |
| 24 | **Bug:** CLI workflow commands used non-existent API paths (`/v1/workflows/*`) | High | ✅ DONE |
| 25 | Consolidate duplicate logging modules (`log_utils/logging.py` → re-export from `aitbc_logging.py`) | Medium | ✅ DONE |
| 26 | Make `REPO_DIR` environment-sourced via `AITBC_REPO_DIR` env var | Medium | ✅ DONE |
| 27 | Add `session.rollback()` in PoA tx processing exception handler for partial-failure safety | Medium | ✅ DONE |

---

## Verification Results

| Check | Result |
|---|---|
| `mypy aitbc/` | **0 errors** |
| `mypy` with `--warn-unused-ignores` (bypassing excludes) | **0 errors** |
| `ruff check aitbc/` | **All checks passed** |
| coordinator-api route audit | **378 routes, 0 duplicates** |
| `aitbc-recovery.service` | **Loaded: loaded (linked; preset: enabled)** |

---

## Detailed Changes

### 1. Queue system type fixes (`aitbc/queues/`)

**Files changed:**
- `aitbc/queues/decorators.py` — Typed `last_called: list[float]` and `timer: list[asyncio.Task[Any] | None]`, removed unreachable `assert`, fixed task creation/awaiting logic
- `aitbc/queues/worker.py` — Fixed implicit Optional: `kwargs: dict[str, Any] | None = None`
- `aitbc/queues/task.py` — Fixed implicit Optional: `kwargs: dict[str, Any] | None = None`
- `aitbc/queues/scheduler.py` — Fixed implicit Optional: `kwargs: dict[str, Any] | None = None`

### 2. Blockchain service response typing (`aitbc/blockchain/blockchain_service.py`)

**Problem:** `AITBCHTTPClient.get()` and `.post()` return `dict[str, Any]` (already parsed JSON), but code was calling `.json()` on the result.

**Fix:** Removed all redundant `.json()` calls. Cast `tx_hash` to `str` to fix `no-any-return`.

### 3. Database connection type fixes (`aitbc/database/`)

**Files changed:**
- `aitbc/database/connection.py` — Typed `self._connection: sqlite3.Connection | None`, used local variable with `assert` in `connect()`, removed unused `type: ignore`
- `aitbc/database/replica.py` — Typed `self.primary_engine: Any`, annotated `self.replica_engines: list[Any]`, added return types to `get_read_engine`, `get_write_engine`, `get_session`, `_setup_monitoring`

### 4. Distributed tracing import fixes (`aitbc/distributed_tracing.py`)

**Fix:** Added `type: ignore[import-not-found]` for Jaeger and HTTPX OpenTelemetry instrumentor imports (missing library stubs). Typed `_tracer` and `_provider` as `Any`. Added return types to `get_tracer`, `start_span`, `end_span`. Removed redundant `type: ignore[union-attr]`.

### 5. Agent bridge integration layer (`aitbc/agent_bridge/src/integration_layer.py`)

**Fix:** Typed `self.session: aiohttp.ClientSession | None`. Added `assert self.session is not None` guards before all `self.session.get()`/`.post()` calls. Wrapped `response.json()` with `dict()` to satisfy `no-any-return`.

### 6. API utilities (`aitbc/api_utils.py`)

**Fix:** Fixed implicit Optional defaults in `build_cors_headers` (`allowed_origins`, `allowed_methods`, `allowed_headers`) and `sanitize_response` (`sensitive_fields`).

### 7. Tracing module (`aitbc/tracing.py`)

**Fix:** Added `type: ignore[import-not-found]` for HTTPX instrumentor import. Changed `_tracer` and `_tracer_provider` from `object | None` to `Any` to allow `start_as_current_span` calls. Changed `get_tracer()` return type to `Any`.

### 8. Agent trading and compliance (`aitbc/agent_trading/`, `aitbc/agent_compliance/`)

**Files changed:**
- `aitbc/agent_trading/src/trading_agent.py` — Fixed `type: ignore` code from `import-not-found` to `import-untyped`. Cast `bool()` on `stop()` return and `dict()` on `get_status()` return.
- `aitbc/agent_compliance/src/compliance_agent.py` — Same `type: ignore` fix. Cast `bool()` on `stop()` return. Rewrote `get_status()` to create a new dict from the bridge response.

### 9. Ethereum RPC, access control, price oracle

**Files changed:**
- `aitbc/ethereum_rpc.py` — Typed `self._w3: Any = None` (was untyped `None`, causing assignment incompatibility with `Web3`). Added `-> Any` return type to `_get_web3()`.
- `aitbc/access_control.py` — Added `type: ignore[arg-type]` for `jwt.encode()` and `jwt.decode()` calls where `secret_key` is `str | None`.
- `aitbc/oracles/price_oracle.py` — Cast `json.load()` result to `dict[str, Any]` to fix `no-any-return`. Added `cast` to imports.

### 10. Agent registry discovery (`aitbc/agent_registry/src/discovery.py`)

**Fix:** Fixed implicit Optional in `find_agents_by_capability` and `find_agents_by_type` (`filters: dict[str, Any] | None = None`). Updated `_matches_filters` signature to accept `dict[str, Any] | None`. Added `Any` import.

### 11. Config module (`aitbc/config/__init__.py`, `aitbc/config/hierarchical_config.py`)

**Fix:**
- Migrated deprecated `TypeAlias` syntax to `type` annotations (UP040)
- Removed `TypeAlias` from imports
- Cleaned up unused `type: ignore` codes (`misc`, `assignment`)
- Added `type: ignore[import-untyped]` for `yaml` import in `hierarchical_config.py`

### 12. Agent registry tests (`aitbc/agent_registry/tests/`)

**Fix:** Added `type: ignore[import-not-found]` on the first `import app` / `from app import ...` in each test file. Subsequent imports in the same file don't need the ignore (mypy resolves `app` from the first import). These imports work at test runtime (when CWD is the agent_registry directory) but mypy cannot resolve them during static analysis from the project root.

### 13. Additional type fixes (agent_protocols, events, circuit_breaker, stage_runner, registration)

**Files changed:**
- `aitbc/agent_protocols/src/task_manager.py` — Added type annotations for `Task.completed_at: datetime | None`, `Task.result: dict[str, Any] | None`, `Task.error: str | None` (were inferred as `None` type, causing assignment incompatibility)
- `aitbc/agent_protocols/src/message_protocol.py` — Typed `self.messages: list[dict[str, Any]]` and `self.received_messages: list[dict[str, Any]]` (were untyped `[]`)
- `aitbc/events/events.py` — Fixed `Event.timestamp: datetime | None = None` (was `datetime = None`, making `__post_init__` check unreachable)
- `aitbc/network/circuit_breaker.py` — Typed `self.open_time: datetime | None = None` (was untyped `None`, causing unreachable code in `check()` and assignment incompatibility in `record_failure()`)
- `aitbc/training_setup/stage_runner.py` — Added `dict[str, Any]` annotation for `results` dict (was inferred with narrow `list[<nothing>]` for `commands` key, causing `attr-defined` on `.append()`)
- `aitbc/agent_registry/src/registration.py` — Fixed implicit Optional for `metadata: dict[str, Any] | None = None`. Added `Any` import.

### 16. Duplicate route registration in coordinator-api

**Problem:** `agent_router` (AI Agent workflow management) was included twice in `apps/coordinator-api/src/app/main.py`:
- At line 313: `app.include_router(agent_router, prefix="/v1")` → routes at `/v1/workflows`, `/v1/executions`, etc.
- At line 378: `app.include_router(agent_router, prefix="/v1/agents")` → same routes at `/v1/agents/workflows`, `/v1/agents/executions`, etc.

**Fix:** Removed the redundant inclusion at line 313. The router is now only mounted at `/v1/agents` which is the correct, more specific prefix.

**Verification:** App starts successfully with 378 routes and 0 duplicates.

### 18. aitbc-recovery systemd symlink

**Problem:** The service file exists at `/opt/aitbc/scripts/utils/aitbc-recovery.service` but was not symlinked into `/etc/systemd/system/`. Systemd showed `Loaded: not-found` despite the service being active.

**Fix:** Created symlink:
```
/etc/systemd/system/aitbc-recovery.service → /opt/aitbc/scripts/utils/aitbc-recovery.service
```
Ran `systemctl daemon-reload`. Service now shows `Loaded: loaded (...; linked; preset: enabled)`.

### 19. Stale `type: ignore` cleanup (`--warn-unused-ignores`)

**Problem:** Running `mypy --warn-unused-ignores` on all `aitbc/` files (bypassing `pyproject.toml` excludes) flagged 18 `unused-ignore` errors in agent registry test files. The `type: ignore[import]` code was too broad — mypy wanted the specific `import-not-found` code. Additionally, once mypy resolves `app` from the first import in a file, subsequent `type: ignore` comments on `import app` in the same file become unused.

**Fix:**
- Changed `type: ignore[import]` → `type: ignore[import-not-found]` on the **first** `import app` / `from app import ...` in each test file
- **Removed** `type: ignore` from all subsequent `import app` statements in the same file (mypy already resolved the module from the first import)
- Verified that `type: ignore` comments in `aitbc_logging.py`, `log_utils/logging.py`, `caching/blockchain_cache.py`, and `network/web3_utils.py` are still needed — no changes required there

**Files changed:**
- `aitbc/agent_registry/tests/test_unit_agent_registry.py` — 1 import: kept ignore with corrected code
- `aitbc/agent_registry/tests/test_integration_agent_registry.py` — 9 imports: kept ignore on first, removed from 8 subsequent
- `aitbc/agent_registry/tests/test_edge_cases_agent_registry.py` — 8 imports: kept ignore on first, removed from 7 subsequent

### 20. Bug: broken `AgentServiceBridge` import in trading + compliance agents

**Problem:** Both `aitbc/agent_trading/src/trading_agent.py` and `aitbc/agent_compliance/src/compliance_agent.py` imported `AgentServiceBridge` from `apps.agent_services.agent_bridge.src.integration_layer`. That module **does not exist** — `apps/agent_services/` is not present in the repo. The actual class lives at `aitbc/agent_bridge/src/integration_layer.py`. Both modules crashed with `ModuleNotFoundError: No module named 'apps.agent_services'` on import. The `# type: ignore[import-untyped]` comment (added during the type-safety pass) was *masking* the broken import from mypy.

**Root cause:** During the v0.5.11 type pass the import error was silenced with `type: ignore` instead of fixing the path. This is a reminder that `import-untyped` / `import-not-found` ignores can hide real runtime breakage.

**Fix:** Changed the import to `from aitbc.agent_bridge.src.integration_layer import AgentServiceBridge` in both files and removed the now-unnecessary `type: ignore`. Verified both modules import cleanly at runtime and the bridge method names (`start_agent`, `stop_agent`, `get_agent_status`, `execute_agent_task`) match the call sites.

### 21. Bug: biased/bursty read-replica routing

**Problem:** `ReadReplicaManager.get_read_engine()` used `hash(time.time()) % 100 >= self.read_weight` to decide whether to route a read to a replica or the primary. `hash()` of a float is not a uniform PRNG — measured distribution was ~68% vs the configured 70% target. Worse, in tight synchronous loops consecutive `time.time()` calls return identical values, producing identical hashes and therefore **identical routing decisions in bursts** (all reads hit the same engine, then flip), defeating weighted load balancing under bursty traffic.

**Fix:** Replaced with `random.randint(0, 99) >= self.read_weight` and added `import random`. Distribution now matches the configured weight and successive calls are independent.

### 22. Bug: `debounce` decorator raised `CancelledError` to superseded callers

**Problem:** In `aitbc/queues/decorators.py`, each `debounce`d call cancels the previous pending task and `await`s its own task. When a newer call supersedes an older one, the older caller's `await task` raised `asyncio.CancelledError` instead of being silently debounced. Verified with a reproduction: caller 1 received `CancelledError` while caller 2 returned its value.

**Fix:** Wrapped `await task` in a `try/except asyncio.CancelledError` that returns `None` for superseded calls — the conventional debounce semantics. Verified: superseded caller now returns `None`, latest caller returns its value.

### 23. Bug: PoA block proposer recorded wrong nonce for confirmed transactions

**Problem:** In `apps/blockchain-node/src/aitbc_chain/consensus/poa.py`, the `Transaction` record was created with `nonce=sender_account.nonce - 1`. The `sender_account` ORM object is loaded before `state_transition.apply_transaction()` runs a raw SQL `UPDATE account SET nonce = nonce + 1`. Raw SQL updates do not refresh the ORM object in the identity map, so `sender_account.nonce` still holds the **pre-increment** value. The `- 1` was presumably intended to "undo" the increment, but since the ORM object was never refreshed, it double-subtracts:

- First-time sender (nonce=0): `0 - 1 = -1` (negative nonce in DB)
- Sender with nonce=5: `5 - 1 = 4` (should be 5 — the nonce that was validated and used)

**Fix:** Changed to `nonce=tx_data_for_transition["nonce"]`, which holds the exact nonce that was validated by `state_transition.validate_transaction()` and used for the state transition. This is the correct nonce for the confirmed transaction record.

### 24. Bug: CLI workflow commands used non-existent API paths

**Problem:** `cli/aitbc_cli/commands/workflow.py` called `/v1/workflows/execute`, `/v1/workflows/{name}/status`, and `/v1/workflows/{name}/stop`. The agent router (mounted at `/v1/agents`) defines `/workflows/{workflow_id}/execute`, `/executions/{execution_id}/status`, and `/workflows/{workflow_id}/cancel`. These paths never matched — the CLI workflow commands were already broken before v0.5.11.

**Fix:** Updated all three CLI commands to use the correct API paths:
- `run`: `POST /v1/agents/workflows/{workflow_id}/execute` with `AgentExecutionRequest` payload
- `status`: `GET /v1/agents/executions/{execution_id}/status` with `AgentExecutionStatus` response fields
- `stop`: `POST /v1/agents/workflows/{workflow_id}/cancel?execution_id=...`

### 25. Consolidate duplicate logging modules

**Problem:** `aitbc/aitbc_logging.py` (432+ importers) and `aitbc/log_utils/logging.py` (only `tests/test_imports.py`) contained near-identical copies of `StructuredFormatter`, `_get_log_level()`, `setup_logger()`, `configure_logging()`, `log_context()`, and `LogContext`. Risk: bugs fixed in one module but not the other.

**Fix:** Replaced `log_utils/logging.py` with a thin re-export shim from `aitbc_logging.py`. `BlockchainTextFormatter` is now an alias for `JournalFormatter` (identical output format). Updated `log_utils/__init__.py` to export `JournalFormatter` and `configure_uvicorn_logging` as well. All existing imports continue to work unchanged.

### 26. Make `REPO_DIR` environment-sourced

**Problem:** `aitbc/constants.py` hardcoded `REPO_DIR = Path("/opt/aitbc")`. If the repo is ever moved, multiple services break silently.

**Fix:** Changed to `REPO_DIR = Path(os.environ.get("AITBC_REPO_DIR", "/opt/aitbc"))`. Defaults to `/opt/aitbc` for backward compatibility. Set `AITBC_REPO_DIR` env var to override.

### 27. Add `session.rollback()` in PoA tx processing exception handler

**Problem:** In `poa.py`'s `_propose_block()`, the per-transaction exception handler (line 323) logged the error and continued to the next tx without rolling back. If `apply_transaction()` partially modified state via raw SQL before failing, those changes would persist in the session and could corrupt the block commit.

**Fix:** Added `session.rollback()` before `continue` in the exception handler. This ensures any partial state changes from a failed transaction are discarded before processing the next transaction.

---

## Known Issues / Suggestions (not changed in this release)

These were identified during the deeper audit but left unchanged because they require product decisions, specs, or test coverage before modifying. Recommended follow-ups:

- **`stage_runner.validate_conditions()` is a no-op stub.** It always returns `passed: True` (with `"actual": "validation_pending"`) regardless of real values/regex. Stage validation in the training runner currently never actually validates anything. *Suggestion:* implement real value/regex comparison against captured command output, or clearly mark as WIP.
- **`ReadReplicaManager.get_session()` rebuilds a `sessionmaker` on every call.** *Suggestion:* construct one `sessionmaker` per engine at init and reuse, to avoid per-call overhead.
- **`EventBus.publish_sync()` uses `asyncio.run()`.** This raises `RuntimeError` if called from within a running event loop (and `publish_event()` relies on it). *Suggestion:* detect a running loop and schedule the coroutine instead, or document that the sync helper is for non-async contexts only.
- **`AgentMessageClient.receive_messages()` uses `message not in self.received_messages`** — O(n) dict-equality scan per message. *Suggestion:* track delivered `message_id`s in a `set` for O(1) membership.
- **Duplicate logging modules.** ~~`aitbc/aitbc_logging.py` and `aitbc/log_utils/logging.py` contain near-identical logic.~~ **Fixed in §25** — `log_utils/logging.py` is now a re-export shim.
- **`type: ignore` on imports can mask runtime breakage** (see §20). *Suggestion:* add a CI step that imports every top-level module (or runs `pytest --collect-only`) so broken imports fail fast, independent of mypy.
- **CLI workflow commands use non-existent API paths.** ~~`cli/aitbc_cli/commands/workflow.py` calls `/v1/workflows/*`~~ **Fixed in §24** — updated to use `/v1/agents/workflows/*` and `/v1/agents/executions/*`.
- **Hardcoded deployment paths in `aitbc/constants.py`.** ~~`REPO_DIR = Path("/opt/aitbc")` is hardcoded.~~ **Fixed in §26** — now sources from `AITBC_REPO_DIR` env var with `/opt/aitbc` fallback.
- **PoA state transition safety.** `apply_transaction()` modifies account balances via raw SQL and flushes, then the block + transactions are committed together in one DB transaction. If `session.commit()` fails, SQLAlchemy rolls back all changes — so state consistency is maintained by the database transaction. **Fixed in §27** — added explicit `session.rollback()` in the per-tx exception handler for partial-failure safety.
- **`pyproject.toml` already has `warn_unused_ignores = true`** (line 173). The user's analysis claiming it was missing was incorrect. The 36 regex excludes are for incremental migration and are being progressively reduced.
- **Circuit breaker boundary.** Both `aitbc/network/circuit_breaker.py` and the PoA-internal `CircuitBreaker` (poa.py:47-78) are safe: `is_open`/`_state` is only set to open after `record_failure()` sets the timestamp, so `check()`/`state` never evaluates with an open state and null/zero timestamp.

---

## Files Changed (Complete List)

| File | Changes |
|---|---|
| `aitbc/queues/decorators.py` | Type annotations for `last_called`, `timer`; **debounce `CancelledError` fix** |
| `aitbc/queues/worker.py` | Implicit Optional fix for `kwargs` |
| `aitbc/queues/task.py` | Implicit Optional fix for `kwargs` |
| `aitbc/queues/scheduler.py` | Implicit Optional fix for `kwargs` |
| `aitbc/blockchain/blockchain_service.py` | Removed `.json()` calls, cast `tx_hash` to `str` |
| `aitbc/database/connection.py` | Type annotation for `_connection`, removed unused `type: ignore` |
| `aitbc/database/replica.py` | Type annotations for `primary_engine`, `replica_engines`, method return types; **`random` routing fix** |
| `aitbc/distributed_tracing.py` | Import stubs `type: ignore`, `_tracer`/`_provider` as `Any`, return types |
| `aitbc/agent_bridge/src/integration_layer.py` | `session` type annotation, `assert` guards, `dict()` wrapping |
| `aitbc/api_utils.py` | Implicit Optional fixes in `build_cors_headers`, `sanitize_response` |
| `aitbc/tracing.py` | Import stub `type: ignore`, `_tracer`/`_tracer_provider` as `Any` |
| `aitbc/agent_trading/src/trading_agent.py` | **Fixed broken `AgentServiceBridge` import**, `bool()`/`dict()` casts |
| `aitbc/agent_compliance/src/compliance_agent.py` | **Fixed broken `AgentServiceBridge` import**, `bool()`/`dict()` casts |
| `aitbc/ethereum_rpc.py` | `_w3: Any` type annotation, `_get_web3` return type |
| `aitbc/access_control.py` | `type: ignore[arg-type]` for JWT calls |
| `aitbc/oracles/price_oracle.py` | `cast()` for `json.load()`, added `cast` import |
| `aitbc/agent_registry/src/discovery.py` | Implicit Optional fix, `_matches_filters` signature, `Any` import |
| `aitbc/agent_registry/src/registration.py` | Implicit Optional fix for `metadata`, `Any` import |
| `aitbc/config/__init__.py` | `TypeAlias` migration, unused `type: ignore` cleanup |
| `aitbc/config/hierarchical_config.py` | `type: ignore[import-untyped]` for `yaml` |
| `aitbc/agent_protocols/src/task_manager.py` | Type annotations for `completed_at`, `result`, `error` |
| `aitbc/agent_protocols/src/message_protocol.py` | Type annotations for `messages`, `received_messages` |
| `aitbc/events/events.py` | `timestamp: datetime | None = None` |
| `aitbc/network/circuit_breaker.py` | `open_time: datetime | None = None` |
| `aitbc/training_setup/stage_runner.py` | `dict[str, Any]` annotation for `results` |
| `aitbc/agent_registry/tests/test_unit_agent_registry.py` | `type: ignore[import-not-found]` on first import only |
| `aitbc/agent_registry/tests/test_integration_agent_registry.py` | `type: ignore[import-not-found]` on first import, removed from 8 subsequent |
| `aitbc/agent_registry/tests/test_edge_cases_agent_registry.py` | `type: ignore[import-not-found]` on first import, removed from 7 subsequent |
| `apps/coordinator-api/src/app/main.py` | Removed duplicate `agent_router` inclusion |
| `apps/blockchain-node/src/aitbc_chain/consensus/poa.py` | **Fixed nonce off-by-one in Transaction record**; added `session.rollback()` in tx exception handler |
| `cli/aitbc_cli/commands/workflow.py` | **Fixed API paths** from non-existent `/v1/workflows/*` to `/v1/agents/workflows/*` and `/v1/agents/executions/*` |
| `aitbc/log_utils/logging.py` | **Replaced with re-export shim** from `aitbc_logging.py` |
| `aitbc/log_utils/__init__.py` | Added `JournalFormatter` and `configure_uvicorn_logging` exports |
| `aitbc/constants.py` | **Made `REPO_DIR` env-sourced** via `AITBC_REPO_DIR` env var |
| `/etc/systemd/system/aitbc-recovery.service` | Created symlink to `scripts/utils/aitbc-recovery.service` |

---

## Post-Release Changes (v0.5.12 Agent B)

The following changes were made after the v0.5.11 release, extending it with database performance indexes, configuration consolidation, dead code removal, and large-file decomposition. All changes are in Agent B's domain (`apps/`, `cli/`, `packages/aitbc-shared/`, systemd).

### Commits

| Commit | Description |
|--------|-------------|
| `5d807d7ec` | `perf(db): add query performance indexes to coordinator-api domain models` |
| `00ab4ab25` | `refactor(cli): remove stale duplicate dual_mode_wallet_adapter.py` |
| `60c2522c2` | `refactor(explorer): decompose blockchain-explorer main.py into routers package` |
| `2f629910f` | `refactor(exchange): decompose simple_exchange_api.py and remove dead api/ package` |
| `7ce7484e6` | `refactor(config): consolidate DatabaseConfig via db_filename extension hook` |
| `96de61900` | `refactor(config): repoint jwt_auth to unified config, delete dead config_pg + db_pg` |
| `0ab95b4a3` | `refactor(config): remove dead effective_database_url and test_database_url` |

### Database Performance Indexes (`5d807d7ec`)

**Problem**: 35 hot columns (status, created_at, recorded_at, moderation_status, submission_time, creation_time) across 10 coordinator-api domain model files were frequently filtered/ordered in SQL queries but lacked indexes. Additionally, 11 composite indexes on the agent_identity tables (cross_chain_mappings, identity_verifications, agent_wallets) were commented out and never enabled.

**Fix**: Added `index=True` to all 35 single-column indexes, chosen by cross-referencing actual SQL query patterns (`.where()`/`.order_by()`) in the storage/router/service layers. Enabled all 11 composite indexes. Added a new `expires_at` index for the identity verification expiry sweep query.

**Alembic migration**: `apps/coordinator-api/alembic/versions/add_query_performance_indexes.py` — uses `if_not_exists=True` for idempotency, since `SQLModel.metadata.create_all` only adds indexes to fresh DBs.

**Verification**: All 46 expected indexes confirmed present via `SQLModel.metadata.create_all` on in-memory SQLite (174 total indexes in schema).

**Files changed** (10 domain + 1 migration):
- `apps/coordinator-api/src/app/domain/agent.py` — `AgentExecution.status`, `AgentStepExecution.status`
- `apps/coordinator-api/src/app/domain/bounty.py` — `Bounty.status`/`creation_time`, `BountySubmission.status`/`submission_time`, `AgentStake.status`
- `apps/coordinator-api/src/app/domain/trading.py` — `TradeRequest`/`TradeMatch`/`TradeNegotiation` status + created_at
- `apps/coordinator-api/src/app/domain/analytics.py` — `MarketMetric.recorded_at`, status+created_at on `MarketInsight`/`AnalyticsAlert`/`AnalyticsReport`/`DashboardConfig`/`DataCollectionJob`/`AlertRule`
- `apps/coordinator-api/src/app/domain/certification.py` — status on `AgentCertification`/`VerificationRecord`/`PartnershipProgram`/`AgentPartnership` + `PartnershipProgram.created_at`
- `apps/coordinator-api/src/app/domain/rewards.py` — `RewardDistribution`/`RewardMilestone` status + created_at
- `apps/coordinator-api/src/app/domain/reputation.py` — `CommunityFeedback.moderation_status`/`created_at`
- `apps/coordinator-api/src/app/domain/community.py` — `AgentSolution.status`, `CommunityPost.created_at`
- `apps/coordinator-api/src/app/domain/developer_platform.py` — `BountyTask.created_at`
- `apps/coordinator-api/src/app/contexts/agent_identity/domain/agent_identity.py` — `AgentIdentity.status`, enabled 11 composite indexes, new `expires_at` index
- `apps/coordinator-api/alembic/versions/add_query_performance_indexes.py` — new migration

### CLI Duplicate Removal (`00ab4ab25`)

**Problem**: `cli/utils/dual_mode_wallet_adapter.py` (626 lines) was a stale pre-package-layout copy of `cli/aitbc_cli/utils/dual_mode_wallet_adapter.py`. All importers used the `aitbc_cli.utils` version; nobody imported the `cli.utils` version.

**Fix**: Deleted the stale copy. Also fixed a missing trailing newline in `cli/aitbc_cli/commands/explorer.py` flagged by ruff.

### Blockchain Explorer Decomposition (`60c2522c2`)

**Problem**: `apps/blockchain-explorer/main.py` was 1,442 lines with all 19 API routes defined inline.

**Fix**: Split into 11 files:
- `validation.py` (29 lines) — SSRF validation patterns (`TX_HASH_PATTERN`, `CHAIN_ID_PATTERN`)
- `models.py` (29 lines) — Pydantic request models (`TransactionSearch`, `BlockSearch`, `AnalyticsRequest`)
- `chain_client.py` (338 lines) — RPC/DB helpers, config constants, `normalize_block()`, `get_chain_head()`, `get_transaction()`, `get_block()`, `get_latest_blocks()`
- `routers/chains.py` (32 lines) — 2 routes
- `routers/blocks.py` (352 lines) — 5 routes
- `routers/transactions.py` (176 lines) — 3 routes
- `routers/analytics.py` (317 lines) — 5 routes
- `routers/search.py` (126 lines) — 2 routes
- `routers/export.py` (120 lines) — 2 routes
- `main.py` (54 lines) — app factory with `include_router` calls, `/health` route, uvicorn entrypoint

All 19 API routes preserved with identical paths, signatures, and response formats. Uses absolute imports since the hyphenated directory name prevents standard package imports.

### Exchange Decomposition + Dead Code Removal (`2f629910f`)

**Problem**: `apps/exchange/simple_exchange_api.py` was 1,209 lines (stdlib `http.server`) with all handlers inline. Additionally, `apps/exchange/api/` (4 files, 422 lines) was a dead previous decomposition attempt — zero importers anywhere in the codebase.

**Fix**: Split `simple_exchange_api.py` into 3 files:
- `db.py` (125 lines) — `get_db_path()`, `init_db()`, `create_mock_trades()`
- `handlers.py` (1,040 lines) — `BaseHandler` (shared `send_json_response`/`do_OPTIONS`/`_read_json_body`), `ExchangeAPIHandler`, `WalletAPIHandler`
- `simple_exchange_api.py` (38 lines) — `run_server()` + argparse entrypoint

Deleted the dead `apps/exchange/api/` package (`__init__.py`, `database.py`, `handlers.py`, `server.py`). The `api/database.py` content survived as `apps/exchange/db.py` (renamed by git). Tests use `exchange_api.py` (FastAPI version), unaffected.

### DatabaseConfig Consolidation (`7ce7484e6`)

**Problem**: `DatabaseConfig` with `effective_url` was copy-pasted in 4 locations: `packages/aitbc-shared/aitbc_shared/core/config.py` (canonical), `apps/agent-management/src/app/core/config.py`, `apps/coordinator-api/src/app/config.py`, `apps/edge/src/aitbc_edge/config.py`. Each service reimplemented the adapter URL formatter with per-service DB filenames, causing drift.

**Fix**: Added a `db_filename: str = "service.db"` class attribute to the shared `DatabaseConfig`. The shared `effective_url` now uses it for both SQLite (`f"sqlite:///{DATA_DIR}/data/{db_filename}"`) and PostgreSQL (`f"{adapter}://localhost:5432/{db_filename.removesuffix('.db')}"`). Services set only `db_filename` instead of reimplementing `effective_url`.

**Per-service changes**:
- **agent-management**: `db_filename = "agent_management.db"`; dropped 12-line `effective_url` override, redundant `model_config`, `DATA_DIR` import
- **coordinator-api**: `db_filename = "coordinator.db"`; dropped 12-line `effective_url` override; extracted 4 repeated `os.getenv("ENVIRONMENT", ...)` calls into `_is_production()` helper; simplified `get_effective_database_url()` to delegate to `database.effective_url`
- **edge**: Replaced hardcoded `postgresql+asyncpg://...:password@localhost:5432/aitbc_edge` with shared adapter pattern (`db_filename="aitbc_edge.db"`, SQLite by default, postgres via env var); replaced placeholder JWT secret `"your-secret-key-change-in-production"` with empty string (must be set via env); fixed `main.py` to use `app_host`/`app_port` from shared `ServiceSettings` instead of non-existent `api_host`/`api_port`
- **shared-core**: Converted to thin re-export shim from `aitbc_shared`

### JWT Auth Repoint + Dead Config Removal (`96de61900`)

**Problem**: `apps/coordinator-api/src/app/auth/jwt_auth.py` imported settings from `config_pg.py` (a legacy PostgreSQL-only config), not the unified `config.py`. `config_pg.py` and `storage/db_pg.py` (343 lines combined) were dead code with only each other as importers.

**Fix**:
- Repointed `jwt_auth.py` import from `..config_pg` to `..config` — JWT fields (`jwt_secret`, `jwt_algorithm`, `jwt_expiration_hours`) are inherited from `BaseAITBCConfig`
- Added `jwt_secret: str = ""` override in unified `Settings` (base defaults to `None` which would break `jwt.encode()`)
- Fixed `database_async.py` `init_async_db()` to use `settings.effective_database_url` (respects test mode) instead of `settings.database.effective_url` (bypassed test mode)
- Deleted `config_pg.py` (98 lines) and `db_pg.py` (245 lines) — zero remaining importers

### Final Config Cleanup (`0ab95b4a3`)

**Problem**: `Settings.effective_database_url` property and `test_database_url` field in `config.py` were dead code after the previous repoint — all call sites now use `settings.database.effective_url` directly.

**Fix**:
- Replaced `settings.effective_database_url` with `str(settings.database.effective_url)` in `database_async.py` (lines 44 and 90)
- Removed `effective_database_url` property from `config.py`
- Removed `test_database_url` field and its env var override
- The test conftest creates its own engine directly (`create_engine("sqlite:///:memory:")`) and never read `settings.effective_database_url`

### DatabaseConfig Consolidation — Phase 2 (uncommitted)

Additional changes completing the B2 config consolidation:

**agent-management**: Added `database: DatabaseConfig = DatabaseConfig()` override on `ServiceSettings` subclass so the agent-management `db_filename = "agent_management.db"` is actually instantiated (previously the inherited base `DatabaseConfig()` was used, ignoring the subclass override).

**coordinator-api `storage/db.py`**: Removed dead `getattr(settings, "database_url", None)` fallback (always returned `None` since `Settings` has no `database_url` attribute) — now uses `settings.database.effective_url` directly.

**edge `storage.py`**: Replaced `os.getenv("DATABASE_URL", "sqlite+aiosqlite:///var/lib/aitbc/data/edge.db")` with `settings.database.effective_url` via `_get_async_db_url()` helper that converts `sqlite://` → `sqlite+aiosqlite://` and `postgresql://` → `postgresql+asyncpg://` for async engine compatibility.

**Verification**: ruff clean, mypy clean (no new errors), runtime smoke tests confirm correct DB URLs for all 3 services (agent_management.db, coordinator.db, aitbc_edge.db).

### Exchange Handler Package Decomposition (uncommitted)

**Problem**: `apps/exchange/simple_exchange/handlers.py` was 1,040 lines with `ExchangeAPIHandler`, `WalletAPIHandler`, and shared helpers all in one file.

**Fix**: Split into a `handlers/` package with mixin-based composition:
- `handlers/base.py` (35 lines) — `BaseHandler`: shared `send_json_response`, `do_OPTIONS`, `_read_json_body`
- `handlers/marketplace.py` (221 lines) — `MarketplaceMixin`: offers/orders CRUD, book, delete
- `handlers/exchange.py` (422 lines) — `ExchangeMixin`: trades, orderbook, place_order, treasury, health, metrics, price, wallet_balance/connect
- `handlers/bridge.py` (278 lines) — `BridgeMixin`: deposit, status, price, withdraw, deposits list, estimate
- `handlers/wallet.py` (32 lines) — `WalletAPIHandler`: balance, connect
- `handlers/__init__.py` (96 lines) — `ExchangeAPIHandler(BaseHandler, MarketplaceMixin, ExchangeMixin, BridgeMixin)` with route dispatch

Also moved `db.py` and `server.py` into the `simple_exchange/` package and replaced `simple_exchange_api.py` with a deprecated shim.

**Verification**: ruff clean, server boots, all routes (`/health`, `/api/orders/orderbook`, `/api/wallet/balance`, `/v1/marketplace/offers`, `/v1/bridge/status`, `/exchange/price.json`) return correct response shapes.

### Systemd Symlink Audit

Verified all 12 `aitbc*.service` symlinks in `/etc/systemd/system/` resolve to non-empty service files. Zero dangling symlinks. (The `aitbc-recovery` symlink was fixed in the original v0.5.11 release.)

### Exchange Security Hardening (uncommitted)

**Body-size guard** (`handlers/base.py`): Upgraded `_read_json_body` with robust Content-Length validation — 411 if header missing, 400 on invalid/negative values, 413 if exceeding 2MB (`MAX_BODY_BYTES`), 400 on `JSONDecodeError`. All `rfile.read` calls now go through this single guarded path.

**X-Api-Key enforcement** (`handlers/base.py` + `exchange.py` + `bridge.py`): Added `_require_api_key()` helper reading `EXCHANGE_API_KEY` env var. Auth disabled when unset (backward compatible). When set, missing/wrong key → 401. Guarded 7 sensitive routes: `handle_place_order`, `handle_treasury_balance`, `handle_wallet_balance`, `handle_wallet_connect`, `handle_bridge_deposit`, `handle_bridge_withdraw`, `handle_bridge_estimate`. Public routes (health, metrics, orderbook, marketplace offers, bridge status) remain unauthenticated.

**Configurable RPC URL** (`handlers/base.py` + `exchange.py`): Replaced per-call `_rpc_base_url()` method with module-level `RPC_BASE_URL` constant reading `BLOCKCHAIN_RPC_BASE_URL` env var (defaults to `http://localhost:9080`). Startup validation: `RuntimeError` if URL doesn't start with `http://` or `https://`. All `urlopen` calls now pass `timeout=RPC_TIMEOUT` (10s) to prevent indefinite hangs on dead RPC endpoints.

**Info leakage fix** (`handlers/exchange.py`): Replaced `send_error(500, str(e))` with `send_error(500, "Internal server error")` in `handle_treasury_balance` and `handle_metrics`.

**Verification**: ruff clean, server boots, 413 on oversized body, 401 on missing/invalid API key (when `EXCHANGE_API_KEY` set), 200 on valid key, auth disabled when unset, `RuntimeError` on misconfigured `BLOCKCHAIN_RPC_BASE_URL`.

### PoA Partial Rollback Fix (uncommitted)

**Problem**: In `apps/blockchain-node/src/aitbc_chain/consensus/poa.py:_propose_block()`, the exception handler called `session.rollback()` — rolling back the **entire session** and discarding all previously committed savepoints from successful transactions in the same block. A single invalid tx in a mixed batch caused loss of valid tx state. Additionally, `begin_nested()` was created *after* recipient account creation, so that mutation was outside the savepoint.

**Fix** (Option A — savepoints):
- Moved `begin_nested()` earlier (before recipient account creation) so the savepoint protects all mutations within a single tx
- Changed exception handler to use `nested.rollback()` when savepoint exists, only falling back to `session.rollback()` for exceptions before savepoint creation
- Added `nested = None` guard at the top of each iteration to track savepoint state

**Verification**: ruff clean, mypy 0 errors, 26/26 consensus tests pass.

### Redis Singleton Hot-Reload Safety (uncommitted)

**Problem**: After uvicorn hot-reload, the module-level `RedisStateManager` singleton persisted with a Redis client bound to the old (closed) event loop. The first request after reload hit `RuntimeError: Event loop is closed` or obscure `ConnectionError`. The `_init()` guard `if self._initialized: return` prevented reconnection.

**Fix** — `apps/coordinator-api/src/app/services/redis_state.py`:
- Added `_loop` attribute to store the event loop reference when `_init()` creates the Redis client
- Added `_is_stale_loop()` method: compares current running loop against stored one
- Added `_reconnect()` method: closes stale client via `aclose()`, resets `_initialized`, calls `_init()` on current loop
- Updated `get_instance()` to check `_is_stale_loop()` and auto-reconnect
- Updated `_init()` to store `self._loop = asyncio.get_running_loop()` after successful ping

**Fix** — `apps/coordinator-api/src/app/main.py`: Lifespan startup now checks `state._is_stale_loop()` first — if stale, calls `_reconnect()` instead of skipping `_init()`.

**Fix** — `apps/coordinator-api/src/app/services/marketplace_cache_optimizer.py`: Same pattern applied to `MarketplaceDataOptimizer` — `_loop` tracking, `_is_stale_loop()`, `_reconnect_if_stale()` called at the top of `get_cached_data`, `set_cached_data`, and `invalidate_namespace`.

**Verification**: ruff clean, mypy 0 new errors, 4/5 Redis integration tests pass (same pre-existing `test_cache_invalidation_on_mutation` failure before and after).

### Post-Release File Impact Summary

| File | Change |
|------|--------|
| `packages/aitbc-shared/aitbc_shared/core/config.py` | **Added `db_filename` extension hook** to shared `DatabaseConfig` |
| `packages/aitbc-shared/aitbc_shared/core/__init__.py` | New — exports `DatabaseConfig`, `ServiceSettings` |
| `packages/aitbc-shared/aitbc_shared/__init__.py` | Added config exports |
| `apps/shared-core/src/app/core/config.py` | **Converted to re-export shim** from `aitbc_shared` |
| `apps/agent-management/src/app/core/config.py` | **Dropped `effective_url` override**, set `db_filename` |
| `apps/coordinator-api/src/app/config.py` | **Dropped `effective_url` override**, set `db_filename`, added `_is_production()` helper, added `jwt_secret` override, removed `effective_database_url`/`test_database_url` |
| `apps/coordinator-api/src/app/auth/jwt_auth.py` | **Repointed import** from `config_pg` to unified `config` |
| `apps/coordinator-api/src/app/database_async.py` | Use `str(settings.database.effective_url)` |
| `apps/coordinator-api/src/app/config_pg.py` | **Deleted** (98 lines, dead code) |
| `apps/coordinator-api/src/app/storage/db_pg.py` | **Deleted** (245 lines, dead code) |
| `apps/coordinator-api/src/app/domain/*.py` (10 files) | **Added 35 single-column indexes** |
| `apps/coordinator-api/src/app/contexts/agent_identity/domain/agent_identity.py` | **Enabled 11 composite indexes**, added `expires_at` index |
| `apps/coordinator-api/alembic/versions/add_query_performance_indexes.py` | **New migration** for index application |
| `apps/edge/src/aitbc_edge/config.py` | **Subclassed shared config**, SQLite fallback, empty JWT secret |
| `apps/edge/src/aitbc_edge/main.py` | Fixed `app_host`/`app_port` from shared `ServiceSettings` |
| `apps/blockchain-explorer/main.py` | **1,442 → 54 lines** (decomposed into 11 files) |
| `apps/blockchain-explorer/validation.py` | New — SSRF validation patterns |
| `apps/blockchain-explorer/models.py` | New — Pydantic request models |
| `apps/blockchain-explorer/chain_client.py` | New — RPC/DB helpers + config |
| `apps/blockchain-explorer/routers/*.py` (6 files) | New — route handlers by domain |
| `apps/exchange/simple_exchange_api.py` | **1,209 → 21 lines** (deprecated shim delegating to `simple_exchange.server`) |
| `apps/exchange/simple_exchange/__init__.py` | New — package marker |
| `apps/exchange/simple_exchange/db.py` | New — DB schema + connection (moved from flat `db.py`) |
| `apps/exchange/simple_exchange/server.py` | New — `run_server()` + argparse entrypoint |
| `apps/exchange/simple_exchange/handlers/__init__.py` | New — `ExchangeAPIHandler` mixin composition + route dispatch |
| `apps/exchange/simple_exchange/handlers/base.py` | New — shared `BaseHandler` with JSON/CORS helpers |
| `apps/exchange/simple_exchange/handlers/marketplace.py` | New — `MarketplaceMixin` |
| `apps/exchange/simple_exchange/handlers/exchange.py` | New — `ExchangeMixin` (trades, metrics, treasury, health, wallet) |
| `apps/exchange/simple_exchange/handlers/bridge.py` | New — `BridgeMixin` (deposit, status, price, estimate) |
| `apps/exchange/simple_exchange/handlers/wallet.py` | New — `WalletAPIHandler` |
| `apps/exchange/db.py` | **Deleted** (moved to `simple_exchange/db.py`) |
| `apps/exchange/handlers.py` | **Deleted** (moved to `simple_exchange/handlers/` package) |
| `apps/exchange/api/` (4 files) | **Deleted** (422 lines, dead code) |
| `apps/agent-management/src/app/core/config.py` | **Added `database` field override** to instantiate service-specific `DatabaseConfig` |
| `apps/edge/src/aitbc_edge/storage.py` | **Replaced `os.getenv` with `settings.database.effective_url`** + async driver conversion |
| `cli/utils/dual_mode_wallet_adapter.py` | **Deleted** (626 lines, stale duplicate) |
| `cli/aitbc_cli/commands/explorer.py` | Fixed missing trailing newline |
| `apps/exchange/simple_exchange/handlers/base.py` | **Security hardening**: 2MB body guard, `_require_api_key()`, `RPC_BASE_URL` constant + startup validation, `RPC_TIMEOUT` |
| `apps/exchange/simple_exchange/handlers/exchange.py` | **Security hardening**: API key guards on 4 routes, `RPC_BASE_URL` + `RPC_TIMEOUT` on all urlopen calls, generic 500 error messages |
| `apps/exchange/simple_exchange/handlers/bridge.py` | **Security hardening**: API key guards on 3 bridge routes |
| `apps/blockchain-node/src/aitbc_chain/consensus/poa.py` | **Partial rollback fix**: moved `begin_nested()` before recipient creation, `nested.rollback()` in exception handler |
| `apps/coordinator-api/src/app/services/redis_state.py` | **Hot-reload safety**: `_loop` tracking, `_is_stale_loop()`, `_reconnect()` |
| `apps/coordinator-api/src/app/main.py` | **Hot-reload safety**: lifespan checks stale Redis loop and reconnects |
| `apps/coordinator-api/src/app/services/marketplace_cache_optimizer.py` | **Hot-reload safety**: `_loop` tracking, `_reconnect_if_stale()` on all cache operations |
