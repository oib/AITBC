[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "aitbc-core"
version = "0.16.2"
description = "Headless AITBC core for white-label integrations"
requires-python = ">=3.13"
dependencies = []

[tool.setuptools.packages.find]
where = ["aitbc_core"]
