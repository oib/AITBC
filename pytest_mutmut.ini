[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
minversion = 8.0
addopts = -ra -q --strict-markers --strict-config -x -q -p no:cov -p no:randomly -p no:random-order
testpaths = ["tests/integration/test_agent_coordinator.py"]
pythonpath = ["apps/agent-coordinator/src"]
