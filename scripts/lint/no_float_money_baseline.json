{
  "_comment": "Known float-money violations, recorded so the guard fails only on new ones. This list may shrink and must never grow. Regenerate after fixing something with: python scripts/lint/no_float_money.py --update-baseline. It started at 210 and is now empty, so the guard is a plain gate again -- any entry appearing here is a regression, not inherited debt.",
  "total": 0,
  "violations": {}
}
