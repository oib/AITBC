# =============================================================================
# changelog-fixture.yml — FROZEN input for the release-notes golden-file test
# =============================================================================
# A tiny, stable HARNESS_CHANGELOG.yml with exactly two releases:
#   9.9.0  — a full feature release (changes + ticket refs + migration_notes),
#            exercises the panel/table/chips/ticket-links/operations sections.
#   9.9.1  — a governor-only patch (changes: []), exercises the AC5 stub page.
# Frozen on purpose: the golden files under this directory are diffed against
# `release-notes.sh` output built from THIS file, so it must never track the
# live HARNESS_CHANGELOG.yml (which changes every release). ABS-226 AC2/AC5.
# =============================================================================

schema_version: "1.0.0"

generated_at: "2026-07-12T00:00:00Z"

releases:
  - version: "9.9.0"
    date: "2026-07-12"
    summary: "Fixture feature release: a new skill, an agent tweak, and a breaking rename to exercise every release-notes section (ABS-226 golden input)."
    upgrade_doc: null
    changes:

      - path: ".claude/skills/demo/SKILL.md"
        category: METHODOLOGY
        change_type: added
        description: "New /demo skill wiring the fixture flow (ABS-226)."
        breaking: false

      - path: ".claude/agents/qas.md"
        category: AGENT
        change_type: modified
        description: "Exit protocol clarified for the fixture (ABS-209, ABS-226)."
        breaking: false

      - path: ".claude/commands/old-name.md"
        category: BREAKING
        change_type: renamed
        renamed_from: ".claude/commands/older-name.md"
        description: "Renamed older-name.md to old-name.md (ABS-226)."
        breaking: true
        migration_action: "Rename older-name.md to old-name.md in your fork."

    migration_notes:
      - "Renamed: older-name.md -> old-name.md. Update any local references."

  - version: "9.9.1"
    date: "2026-07-13"
    summary: "Governor-only patch: no .claude/ harness file changes; marker entry for tag/changelog parity (ABS-226 stub input)."
    upgrade_doc: null
    changes: []
